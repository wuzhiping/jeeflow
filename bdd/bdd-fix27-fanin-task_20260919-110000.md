# BDD 任务 #122: §27 修复验证

## 目标
验证多入边 task 节点在并发/串行场景下只创建 1 个 task（修复前是 2 个）

## 流程
start1 → apply → fork1 → [taskA, taskB] → task_collect(task 节点) → end1

## 修复前
- taskA 完成 → task_collect 创建 #1 (DOING)
- taskB 完成 → task_collect 创建 #2 (DOING)
- director 看到 2 个 task，流程卡 state=10

## 修复方案
- `lock_instance_for_update` 悲观锁（PG FOR UPDATE / MySQL FOR UPDATE / 内存 no-op）
- `_create_task` 入口加 `find_doing_tasks` 去重
- facade 入口包 `with_tx` 事务

## 实测
1. startAndExecute → inst_id ✓
2. leader 完成 taskA ✓
3. manager 完成 taskB ✓
4. **task_collect todo count = 1**（修复前 2）✓
5. director 完成 task_collect → state=20 DONE ✓

## 改动清单
- `vendor/jeeflow/spi.py` +1 抽象方法 `lock_instance_for_update`
- `vendor/jeeflow/repository/base.py` +8 行 JdbcRepository 实现
- `vendor/jeeflow/memory.py` +4 行 no-op + with_tx
- `vendor/jeeflow/engine.py` `_create_task` +6 行入口加锁+去重
- `vendor/jeeflow/facade.py` `_processTask_execute` + `_startAndExecute` 包 with_tx
- `main.py` + `main_pg.py` 修复 vendor 加载顺序（必须在 main_common import 之前）

## 结论
✅ **PASS**：§27 多入边 task 节点去重修复成功
