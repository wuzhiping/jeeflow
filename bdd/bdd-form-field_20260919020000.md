# BDD #99 bdd-form-field (20260919020000)

## 流程设计
```mermaid
flowchart LR
    start([开始]) --> apply[提交表单]
    apply --> leader_review[直属]
    leader_review --> priority_decision{优先级}
    priority_decision -->|≥3| high_handler[高优先]
    priority_decision -->|<3| low_handler[低优先]
    high_handler --> end([结束])
    low_handler --> end
```

## 场景
高优先级表单

## 结果
- memory (8101): ❌ FAIL
- pg (8102): ❌ FAIL
