# BDD Task 52: submitType=5 RE_APPLY 重提（BUG 发现 + 修复）

- **时间**：2026-09-17 15:08:00（TS=20260917150800）
- **JSON 定义**：`./bdd/bdd-reapply_20260917150800.json`

## 1. 场景设计

```mermaid
flowchart LR
    A([开始]) --> B[apply]
    B --> C[leader]
    C --> D([结束])
```

期望：leader 用 RE_APPLY(5) 让 apply 任务重新激活

## 2. 实测（修复前）

| 步骤 | 操作 | 结果 |
|---|---|---|
| 1 | apply AGREE | leader DOING |
| 2 | leader RE_APPLY(5) | **state=20 直接结束** |

### Engine 行为（facade.py L295-318）

```python
elif submit_type == SUBMIT_REJECT:      # 2
    execute_and_jump_to_end(...)
elif submit_type == SUBMIT_ROLLBACK:    # 3
    execute_and_jump_task(...)
elif submit_type == SUBMIT_JUMP:        # 4
    execute_and_jump_task(..., taskName)
elif submit_type == SUBMIT_ROLLBACK_TO_OPERATOR:  # 6
    execute_and_jump_to_first_task_node(...)
else:  # 0 APPLY / 1 AGREE / 5 RE_APPLY
    execute_process_task(...)
```

**submitType=5 没有专门路由**，被当作 AGREE 处理 → leader 完成任务 → 流转到 end → state=20。

## 3. 修复（FIX-T4）

main.py + main_pg.py 同步：
```python
from jeeflow.model import SubmitType
async def _reapply_flow(action, args=None):
    args = dict(args or {})
    if action == "processTask/execute" and int(args.get("submitType") or 1) == int(SubmitType.RE_APPLY):
        args["submitType"] = int(SubmitType.ROLLBACK_TO_OPERATOR)
    return await facade.flow(action, args)
facade.flow = _reapply_flow
```

把 RE_APPLY(5) 路由为 ROLLBACK_TO_OPERATOR(6) - 跳到第一个 task 节点。

## 4. 修复后实测

| 步骤 | 操作 | 结果 |
|---|---|---|
| 1 | apply AGREE | leader DOING actor=leader |
| 2 | leader RE_APPLY(5) | **apply DOING actor=user1** ✅ |

### 历史轨迹

```
apply state=20 actors=['user1']      # 第 1 次（已完成）
leader state=20 actors=['leader']    # leader 完成
apply state=10 actors=['user1']      # RE_APPLY 重新激活
```

## 5. 关键发现

1. **`SUBMIT_RE_APPLY=5` 在 facade 路由表中缺失**：仅 SUBMIT_REJECT/ROLLBACK/JUMP/ROLLBACK_TO_OPERATOR/COUNTERSIGN_DISAGREE 有专门路由
2. **RE_APPLY 期望语义**：让前面 DONE 的 task 重新激活（类似 ROLLBACK 但只回到申请节点）
3. **修复方案**：monkey-patch facade.flow 把 5 替换为 6（ROLLBACK_TO_OPERATOR 语义最接近）
4. **engine.py 中无 SubmitType.RE_APPLY 任何处理代码**

## 6. 修复限制

- facade.py 是只读 jeeflow 包，**实际修复需上游 PR**：
  - facade.py L295-318 增加 elif submit_type == SUBMIT_RE_APPLY 分支
  - engine.py 实现 execute_and_reapply 或复用 execute_and_jump_to_first_task_node
- 当前用 monkey-patch 在 main.py + main_pg.py 实现等效

