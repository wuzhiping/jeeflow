# BDD #191-#195 实例/任务操作 (20260919 184000)

## 测试结果

| # | Action | 结果 | 状态 |
|---|--------|------|------|
| 191 | processInstance/withdraw | state=30 WITHDRAW ✓ | ✅ |
| 192 | processInstance/suspend | 未知 action | ❌ 缺失 |
| 193 | processTask/transfer | 未知 action | ❌ 缺失 |
| 194 | processTask/comment | 未知 action | ❌ 缺失 |
| 195 | processTask/extra | 未知 action | ❌ 缺失 |

## 缺失功能（限制 §70）

- **§70 实例/任务扩展操作**（2026-09-19 确认）
  - `processInstance/suspend` - 实例挂起（state=50 SUSPEND）→ 未实现
  - `processTask/transfer` - 任务转交（指定新 actor）→ 未实现
  - `processTask/comment` - 任务评论（独立于 execute）→ 未实现
  - `processTask/extra` - 任务额外信息（自定义字段）→ 未实现

## 模型层支持（已存在但未暴露）
- `ProcessInstance.state` 包含 `SUSPEND=50`
- `ProcessTask.variables` dict 可存额外信息
- 业务方需通过 `processTask/execute` 的 `comment`/`extra` 字段写入

## 建议
- 如需挂起：临时把 `ProcessInstance.state` 设为 50（自实现 endpoint）
- 如需转交：先 `removeCandidate` + `addCandidate`（已实现）
- 评论/额外信息：通过 `processTask/execute` 的 `comment` 字段

## 结论
- 1 个 action 工作（withdraw）
- 4 个 action 缺失（设计限制 §70）
- 0 个 BUG

## 结果
- memory (8101): ⚠️ 限制确认
