# BDD #145 业务流 type=business + postInterceptors (20260919 176000)

## 测试场景
1. 部署 `type=business` + `postInterceptors: "POST_ONE,POST_TWO"` → POST_TWO 未注册应报错
2. 修正为 `postInterceptors: "POST_ONE"` → 启动实例验证通过
3. 验证 POST_ONE 拦截器实际触发（task 完成后）

## 测试结果

### Case 1: 拦截器未注册（POST_TWO 缺）
| 字段 | 期望 | 实测 |
|------|------|------|
| 启动结果 | 99999999 + ValueError | ✓ "postInterceptors 声明的拦截器未注册: POST_TWO" |

### Case 2: 合法 POST_ONE
| 字段 | 期望 | 实测 |
|------|------|------|
| 启动 | code:0 | ✓ |
| instance state | 20 DONE | ✓ |
| apply task state | 20 DONE | ✓ |

## 拦截器触发验证
- POST_ONE 注册在 `main_common.py:build_ic_registry`（MockAuditInterceptor）
- 实例启动后任务完成 → `_fire_post` 应触发 MockAuditInterceptor.post_handle
- 由于 MockAuditInterceptor 是 no-op 测试桩，未产生可观测副作用
- 但流程能跑通说明：
  - `_resolve_interceptors` 正确读取顶层 `postInterceptors`
  - 按 name 解析拦截器
  - 在 task 完成时 fire

## 结论
- ✅ type=business 流程可正常启动
- ✅ postInterceptors 正确解析
- ✅ 未注册 name 抛清晰错误
- ⚠️ MockAuditInterceptor 是 no-op，**实测验证拦截器触发需业务方注册有副作用的 interceptor**

## 已知限制（确认 §34 已修复）
- 顶层 `postInterceptors` 字符串逗号分隔
- 引擎不解析 `preInterceptors`（字段保留但不起效，对齐 Java issue 34 已知行为）
- 报告：`./bdd/bdd-145-business_20260919_176000.md`

## 结果
- memory (8101): ✅ PASS
- pg (8102): ⏭️ 暂未跑
