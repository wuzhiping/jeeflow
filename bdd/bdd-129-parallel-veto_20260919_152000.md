# BDD #129 PARALLEL 一票否决 修复报告 (20260919 153000 + 2026-09-19 FIX-T46)

## 原始测试（修复前）
**🐛 BUG**: ONE_VOTE_VETO REJECT 时 instance state 跳到 20 (DONE) 而非 45 (REJECTED)
- userA reject submitType=20
- instance.state → 20 DONE（被下游 end 节点的 inst.finish() 覆盖）
- 业务方期望 45 REJECTED

## 修复（FIX-T46 2026-09-19）

**位置**：`vendor/jeeflow/engine.py:execute_process_task:168-180`

**代码**：
```python
if cs_veto:
    from .model import InstanceState
    inst.state = InstanceState.REJECT
    inst.updateTime = now
    await self.repo.update_instance(inst)
    await self._fire_event(ProcessEvent(EventType.PROCESS_REJECT, inst.id, task_id, operator=operator))
    # FIX-T46：否决后不往下游推进（end 节点会覆盖 state=20 DONE）
    return await self.repo.find_instance_by_id(inst.id)
```

**关键**：
1. 设置 `inst.state = REJECT` (45) + `update_instance` 持久化
2. fire PROCESS_REJECT 事件
3. **`return` 跳过下游 _follow_edges**（否则 end 节点的 inst.finish() 会覆盖状态）

## 验证（修复后）

| 字段 | 修复前 | 修复后 |
|------|--------|--------|
| instance.state | 20 DONE | **45 REJECTED** ✓ |
| apply task state | 20 | 20 ✓ |
| userA task state | 20 | 20 ✓ |
| userB task state | 99 | 99 ✓ |
| userC task state | 99 | 99 ✓ |
| userB todo | 有 | 无 ✓ |
| userC todo | 有 | 无 ✓ |

## 结果
- memory (8101): ✅ PASS
- pg (8102): ⏭️ 暂未跑
