# Customer Reset · abc.feg.cn · 20260922100618

> **状态**：✅ **已完成**（AI 执行）
> **SOP**：`ToT/sop/customer-data-reset.md` v0.2
> **与首次 reset 间隔**：~12 分钟（首次 = `202609220955xx` → 本次 = `20260922100618`）

## 基本信息

- 目标服务器：`https://abc.feg.cn/jeeflow/`
- 后端类型：**PG**（共享 `10.17.1.26:6432/litellm`）
- 操作人：**AI（opencode）**
- 触发原因：用户口头指令 "reset abc.feg.cn data again"
- Jira 工单：未填

## 操作前状态

| 表/项 | 行数（清前） |
|-------|---------------|
| wf_process_define | **1**（仅 fdep） |
| wf_process_instance | **3**（doing 冒烟残留） |
| wf_process_task | 3 |
| wf_process_cc_instance | 0 |
| wf_process_task_actor | ? |
| wf_process_surrogate | 0 |
| wf_process_design_his | 0 |
| wf_process_design | 0 |

**doing 实例分布**：`stage_design=1, stage_pm=2`（首次 reset 后的冒烟 + 就绪检查时的冒烟 + 本次 reset 前的一次遗留）

## 执行命令

```bash
# 1. Reset
curl -s -X POST https://abc.feg.cn/jeeflow/api/reset \
    -H "Content-Type: application/json" -d '{}' --max-time 30
# 返回: {"code":0,"msg":"成功","data":{"reloadedDefines":0}}

# 2. 部署 fdep.json（reset 不自动重载 seed flows）
python3 -c "
import json
content = open('/opt/jupyter/src/RD/projects/jeeFlow/ToT/flows/fdep.json').read()
print(json.dumps({'content': content, 'operator': 'system', 'name': 'fdep'}))
" | curl -s -X POST https://abc.feg.cn/jeeflow/wf/processDefine/deploy \
    -H "Content-Type: application/json" -d @- --max-time 30
# 返回: {"code":0,"msg":"成功","data":{"processDefineId":"1790042780958000"}}
```

## 操作后验证

- [x] **TRUNCATE 全部 wf_process_* 表成功**（defines=0, doing=0）
- [x] **fdep 重新部署成功**：`processDefineId=1790042780958000`
- [x] **healthz**: HTTP 200
- [x] **端到端冒烟**：`startAndExecute` 创建 instance=92203808007179

## 实际执行结果

| 时间 | 步骤 | 结果 |
|------|------|------|
| 10:06:18 | 测可达性 | ✅ HTTP 200 |
| 10:06:18 | 盘点 define | 1 行 (fdep) |
| 10:06:18 | 盘点 doing | 3 实例 (stage_design×1, stage_pm×2) |
| 10:06:18 | `POST /api/reset` | ✅ 成功 |
| 10:06:20 | 清后盘点 | 0 defines + 0 doing |
| 10:06:20 | `POST /wf/processDefine/deploy` | ✅ id=1790042780958000 |
| 10:06:21 | healthz | ✅ HTTP 200 |
| 10:06:21 | 端到端冒烟 | ✅ instance=92203808007179 |
| 10:08:xx | **冒烟跑到底**（用户要求） | ✅ state=DONE, 6 阶段全完成 |
| 10:08:xx | doingList 复检 | ✅ instances=0, tasks=0 |

## 注意事项 / 风险

- ⚠️ 第二次 reset 操作，本次**同样无备份**（用户口头授权模式延续）
- ⚠️ 每次 reset 后 fdep 都会被清掉，必须手动 deploy 一次（除非服务重启触发 auto_deploy_fdep）
- ⚠️ **当前每次 reset 都需 AI 介入**，建议长期方案：让客户服务器启动时自动加载 `ToT/flows/fdep.json`（即让 FLOWS=true 时扫描 ToT 目录而非只扫 flows/ 目录）
- ⚠️ 冒烟测试遗留 instance=92203808007179（DOING, stage_intake）

## 当前客户服务器最终态

```
URL              : https://abc.feg.cn/jeeflow/
fdep id          : 1790042780958000 (state=1, reset #2)
doing instances  : 0 (冒烟推到 DONE 后干净)
SPI users        : 14 (含 u_fdp_pm)
SPI roles        : 14 (含 6 fdep_*)
最近完成冒烟     : instance 92203808007179 → state=DONE, 6 阶段全完成
```

## 改进建议（下次 reset 时考虑）

1. **让客户服务器启动时自动加载 `ToT/flows/fdep.json`**：改 `main_pg.py` 或 `flows_resolver.dir()`，让 `FLOWS=true` 时同时扫描 `ToT/flows/` 与 `flows/`。这样 reset 后重启服务即可恢复 fdep，**不需 AI 介入 deploy**。
2. **引入 backup-then-reset 模式**：下次 reset 前先 `pg_dump` 到 `/var/backup/jeeflow/<ts>.sql`，留档。
3. **冒烟 instance 自动清理**：reset 前自动 archive 全部 DOING 实例（或 reset 时直接清）。

---

## 变更记录

| 时间 | 操作 |
|------|------|
| 2026-09-22 10:06 | AI 执行 reset #2 + 手动 deploy fdep + 冒烟验证 |
| 2026-09-22 10:07 | 写本留档 |
