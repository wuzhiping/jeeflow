# 客户服务器 abc.feg.cn 部署留档 · 2026-09-22

> **类型**：新功能部署（pickup API + Job Cards + Decision Mem 协议 v1.2 lite+）
> **前置**：所有本地 SOP 全过（spi-verify / flow-lint / tdd-flow）+ 客户服务器 fdep.json 字节一致
> **执行人**：人工 push + 重启 + AI 自动化验证

---

## 1. 部署内容

| 文件 | 推送方 | 备注 |
|------|--------|------|
| `main.py` + `main_pg.py` + `main_common.py` | 人工 git push | ★ 新增 `executor_pickup()` + `/api/executor/pickup` 路由 |
| `ToT/flows/fdep/job_cards/*.md` (6 张) | 人工 git push | ★ Job Card 仓库 |
| `ToT/sop/gen-job-cards.py` | 人工 git push | 生成器 |
| `ToT/flows/fdep/RESPONSES.md` 等文档 | 人工 git push | 协议 v1.2 lite+ |

---

## 2. 部署前预检（全部通过）

| # | 检查项 | 结果 |
|---|--------|------|
| 1 | 本地 dev 健康 | ✅ UP |
| 2 | 客户服务器健康 | ✅ UP, pg=ok |
| 3 | spi-verify | ✅ PASSED |
| 4 | flow-lint | ✅ PASSED |
| 5 | tdd-flow | ✅ PASSED (happy=DONE, reject=REJECT) |
| 6 | 客户 fdep 部署 | ✅ id=1790042780958000 |
| 7 | fdep.json 字节一致 | ✅ md5 match |

---

## 3. 部署后 AI 自动验证（全部通过）

### 3.1 pickup API 端点探测

```
[GET /healthz]        → {"status":"UP","backend":"python","pg":"ok"}
[POST /api/executor/pickup]
  → code=0, msg=成功
  → task.taskName        = stage_pm
  → jobCard.url          = ToT/flows/fdep/job_cards/job_card_stage_pm.md
  → jobCard.exists       = True
  → jobCard.content_size = 2927 B
  → jobCard.absolutePath = /app/ToT/flows/fdep/job_cards/job_card_stage_pm.md
```

**关键证据**：客户服务器文件系统存在 `/app/ToT/flows/fdep/job_cards/job_card_stage_pm.md`，6 张卡均部署到位。

### 3.2 demo_v4 实跑（pickup → execute → audit）

| 实例 ID | 时间 | 流程 | 状态 |
|---------|------|------|------|
| 92211691247620 | 2026-09-22 ~12:14 | fdep v0.6.1 | ✅ DONE (state=20) |

### 3.3 审计表（5/5 PASS）

| Task | Used (执行用卡) | Next (传递给下一节点) | Audit |
|------|-------------------|------------------------|-------|
| stage_pm | job_card_stage_pm.md | job_card_stage_design.md | ✓ PASS |
| stage_design | job_card_stage_design.md | job_card_stage_dev.md | ✓ PASS |
| stage_dev | job_card_stage_dev.md | job_card_stage_review.md | ✓ PASS |
| stage_review | job_card_stage_review.md | job_card_stage_feedback.md | ✓ PASS |
| stage_feedback | job_card_stage_feedback.md | terminal | ✓ PASS |

### 3.4 链路一致性（5/5 PASS）

```
✓ stage_pm.next → stage_design.used      (jcu 匹配)
✓ stage_design.next → stage_dev.used     (jcu 匹配)
✓ stage_dev.next → stage_review.used     (jcu 匹配)
✓ stage_review.next → stage_feedback.used (jcu 匹配)
✓ stage_feedback.next → (end)            (terminal 标记)
```

---

## 4. 部署结论

**✅ 部署成功，所有功能在客户服务器工作正常**

- 新 API 端点 `/api/executor/pickup` 已上线
- 6 张 Job Card 已部署到客户文件系统
- Decision Mem 协议 v1.2 lite+ 的 `job_card_url` 审计字段端到端可用
- 链路接力机制（next_handoff）正确
- 实例 state=20 完整跑通

## 5. 留档

- 完整报告：`/tmp/demo_v4_customer_report.json`
- 客户实例：`92211691247620`（客户服务器 PG）
- 本地对照：`ToT/tdd/test_fdep_baseline_v3audit.md` (instance 92210956748829)

## 6. 后续建议

| # | 建议 |
|---|------|
| 1 | 监控 pickup API 调用频次（看 executor 是否真在用） |
| 2 | 监控 job_card_url 解析失败率（文件丢失告警） |
| 3 | v0.7+ 引入 SPI 角色解析后，可改造为多用户接力（不再全 u_fdp_pm） |
