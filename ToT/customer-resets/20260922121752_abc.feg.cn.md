# 客户服务器 abc.feg.cn reset #3 · 2026-09-22

> **类型**：数据 reset（PG TRUNCATE wf_process_*）
> **触发**：用户口头指令"reset customer data for next trip"
> **目的**：在新功能（pickup API + Job Cards + 审计字段）部署完成后，清理上次的演示数据，准备下一轮测试

---

## 1. 前置检查

| # | 检查项 | 结果 |
|---|--------|------|
| 1 | healthz | ✅ `{"status":"UP","backend":"python","pg":"ok"}` |
| 2 | 当前 defines 盘点 | 1 个（fdep, version=1） |
| 3 | doingList 盘点 | 0 个（无 DOING 实例，安全 reset） |

---

## 2. reset 执行

```bash
curl -X POST https://abc.feg.cn/jeeflow/api/reset -d '{}'
# 响应: {"code":0,"msg":"成功","data":{"reloadedDefines":0}}
```

**底层行为**（main_pg.py._reset_pg）：
- TRUNCATE 全部 wf_process_* 表
- RESTART IDENTITY（PG 序列重置）
- reloadedDefines=0 → seed reload 跳过，需手动 deploy

---

## 3. 重新部署 fdep.json

```bash
curl -X POST https://abc.feg.cn/jeeflow/wf/processDefine/deploy -d '{
  "content": "<fdep.json 内容>",
  "operator": "system",
  "name": "fdep"
}'
# 响应: {"code":0,"msg":"成功","data":{"processDefineId":"1790050655064000"}}
```

**新 fdep 定义 ID**：`1790050655064000`（reset 前是 `1790042780958000`）

---

## 4. 健康检查

| 检查 | 结果 |
|------|------|
| healthz | ✅ UP |
| fdep 部署 | ✅ id=1790050655064000, version=1 |

---

## 5. 冒烟测试（"跑到底"）

| 实例 ID | 结果 |
|---------|------|
| 92211877435403 | ✅ state=20 (DONE) |

5 阶段全部推过：
- ✓ stage_pm
- ✓ stage_design
- ✓ stage_dev
- ✓ stage_review
- ✓ stage_feedback

doingList 复查 = 0。

---

## 6. 部署后状态

| 项 | 值 |
|----|----|
| fdep defineId | **1790050655064000**（新） |
| 总 defines | 1 |
| 总 instances | 1（DONE） |
| 总 doing | 0 |
| healthz | UP |
| pg | ok |

---

## 7. 注意事项（给下次）

1. **新 fdep defineId 是 1790050655064000**（非 1790042780958000），后续 demo / TDD 测试需用新 ID
2. **客户服务器仍共享本地测试 PG**（`10.17.1.26:6432/litellm`），本地 8102 端也同步清空
3. **Job Cards 仍在 `/app/ToT/flows/fdep/job_cards/`**（引擎 push 时已部署），无需重新 push

## 8. 关联

- `ToT/sop/customer-data-reset.md` v0.3 — 流程规范
- `ToT/customer-checks/2026-09-22_ready.md` — 上次就绪检查
- 之前的 reset 留档：`2026-09-22_abc.feg.cn.md`、`20260922100618_abc.feg.cn.md`
