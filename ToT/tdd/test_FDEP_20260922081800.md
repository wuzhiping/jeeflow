# TDD Test Log · FDEP.json v0.6.2 · 20260922081551

**结论：✅ PASS（happy path）— W012 BUG 修复，6 阶段全 DONE**

---

## 1. 测试对象

- `ToT/flows/FDEP.json` v0.6.2（升级自 v0.5）
- 10 节点 / 10 边 / 1 snaker:decision
- SPI: spi/dev（已 PASSED spi-verify.py）

## 2. v0.5 → v0.6.2 关键变更

| 变更 | 目的 |
|------|------|
| 新增 `decision_intake` (snaker:decision) 节点 | 修 W012 BUG-1 §110 |
| `stage_intake` 双出边 → decision 三出边 | 用 submitType 路由分流 |
| `e_decision_approve` (`submitType==1`) → stage_pm | 立项分支 |
| `e_decision_reject` (`submitType==2`) → end_rejected | 驳回分支 |
| `e_decision_default` (`expr=""`) → stage_pm | FIX-T112 兜底 + 处理 W013 |
| 6 个 task 的 `assignee` 从 R3/R6/R7 → 直接 `u_fdp_pm` | 引擎不自动解析 SPI 角色（缺 assignmentHandler），需直接赋 uid |
| SPI 角色名 (fdep_intake/rml/...) 保留在 text.value 语义层 | 人类可读 + 未来可换 assignmentHandler |

## 3. 静态校验

| 检查器 | 结果 |
|--------|------|
| `tdd/validate_flow.py` | ✅ OK (10 nodes, 10 edges) |
| `vendor/jeeflow/verify_flow` | ⚠️ 0 errors, 2 warnings |
| `ToT/sop/spi-verify.py` | ✅ PASSED |

剩余 warnings（已接受）：
- **W008**: decision_intake → stage_pm 有 2 条边（approve + default，冗余但语义必要）
- **W014**: 含 snaker:decision 节点，FIX-T113 §117 历史 BUG 提醒

## 4. 引擎实跑结果

### Test 1: Happy path（submitType=1 逐 task execute）

```
[08:17:38] ✓ instance: 92197128909825
[08:17:38] step 1: 待办 stage_pm (期望 stage_pm)
[08:17:38] step 2: 待办 stage_design (期望 stage_design)
[08:17:38] step 3: 待办 stage_dev (期望 stage_dev)
[08:17:38] step 4: 待办 stage_review (期望 stage_review)
[08:17:38] step 5: 待办 stage_feedback (期望 stage_feedback)
[08:17:38] [HAPPY.end] state=DONE (20)
[08:17:38] [HAPPY.end] 6 tasks:
  - stage_intake       DONE  op=u_fdp_pm  actors=['u_fdp_pm']
  - stage_pm           DONE  op=u_fdp_pm  actors=['u_fdp_pm']
  - stage_design       DONE  op=u_fdp_pm  actors=['u_fdp_pm']
  - stage_dev          DONE  op=u_fdp_pm  actors=['u_fdp_pm']
  - stage_review       DONE  op=u_fdp_pm  actors=['u_fdp_pm']
  - stage_feedback     DONE  op=u_fdp_pm  actors=['u_fdp_pm']
```

✅ **6 阶段全部 DONE，instance.state=20（完成）**，u_fdp_pm 全程可操作。

### Test 2: Reject path（手动 orchestration 待完善）

driver 误对 stage_pm 发 submitType=2（应在 stage_intake 时主动 REJECT），结果 state=REJECT(45) 但驳回的是 stage_pm 而非 intake。

⚠️ 真正的 reject path 需在 `processInstance/start`（不用 startAndExecute）后，**手动** execute `stage_intake` with `submitType=2`。driver 改造留待下次会话。

## 5. 仍有 P1（不阻塞 happy path）

- 🟡 P1: `variables.u_deptName="默认部"`（应为 "FDEP协作组"），引擎 dept lookup 路径与 SPI 不同
- 🟡 P1: SPI 角色自动解析（v0.7+ 用 `assignmentHandler`），目前用 `u_fdp_pm` 占位
- ⏳ TODO: reject path driver 重写（手动 orchestration）
- ⏳ TODO: 其他阶段驳回扩展（Q5）

## 6. 决策对比

| 项 | v0.5 实跑 | v0.6.2 实跑 |
|----|-----------|-------------|
| instance.state after startAndExecute | DONE(20) ❌ 立刻 | DOING(10) ✅ 正常 |
| stage_pm 是否可被 u_fdp_pm 拾起 | ❌ actors=['R3'] | ✅ actors=['u_fdp_pm'] |
| 6 阶段全 DONE | ❌ 卡 stage_pm | ✅ 全部 DONE |
| W012 警告 | ⚠️ 存在 | ✅ 消失 |
| W013 警告 | — | ✅ 消失（加 default edge） |

## 7. 关联

- `ToT/flows/FDEP.json` v0.6.2 — 被测对象
- `ToT/tdd/test_FDEP_20260922081228.md` — v0.5 实跑日志（FAILED）
- `ToT/README.md#5` — 镜像（v0.6.2 同步）
- `ToT/mapping.md` — R 角色→SPI 角色映射（v1.1 实测落地）

## 8. 变更

| 时间 | 操作 |
|------|------|
| 2026-09-22 08:15 | 实跑 v0.6：发现 FIX-T112 兜底 + 角色未解析两问题 |
| 2026-09-22 08:16 | 改 assignee 为 u_fdp_pm（v0.6.1） |
| 2026-09-22 08:17 | 加 default edge（v0.6.2），happy path PASSED |
| 2026-09-22 08:18 | 写入本测试日志，README §5 + mapping.md 同步 |
