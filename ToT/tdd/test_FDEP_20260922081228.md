# TDD Test Log · FDEP.json · 20260922081228

**结论：❌ FAILED — FDEP.json v0.5 当前不可执行，引擎 BUG-1 §110 重现 + 角色解析断裂**

---

## 1. 被测对象

- 文件：`ToT/flows/FDEP.json`（v0.5）
- 节点：9 个（start / stage_intake / stage_pm / stage_design / stage_dev / stage_review / stage_feedback / end / end_rejected）
- 边：8 条（含 e1_approve + e1_reject 双出边于 stage_intake）
- SPI：`spi/dev`（已 PASSED spi-verify.py）

## 2. 测试环境

- engine：jeeFlow memory backend（main.py 同款初始化）
- operator：`u_fdp_pm`（占位用户）
- 直接调用 `JeeflowFacade.flow(...)`，不开 HTTP
- driver：`/tmp/opencode/run_fdep_v2.py`

## 3. 静态校验（前置）

| 检查器 | 结果 |
|--------|------|
| `tdd/validate_flow.py` | ✅ OK (9 nodes, 8 edges) |
| `vendor/jeeflow/verify_flow` | ⚠️ 0 errors, **1 warning (W012)** |
| `ToT/sop/spi-verify.py` | ✅ PASSED (spi/dev 数据完整) |

W012 内容：
> task 节点[stage_intake] 有 2 条出边且其中 1 条 target 为 end 节点。引擎会遍历所有出边，end 节点被提前遍历导致 instance.state=20 但下游 task 仍 DOING。请用 decision/fork 节点分隔分支。

## 4. 实跑结果（动态执行）

### Test 1: `processDefine/startAndExecute`

```json
{
  "code": 0,
  "instance_id": "92196795698177",
  "instance_state": "DONE (20)",          ← 异常：刚启动就 DONE
  "tasks": [
    {"name": "stage_intake", "state": "DONE", "operator": "u_fdp_pm", "actors": ["R3", "u_fdp_pm"]},
    {"name": "stage_pm",     "state": "DOING", "operator": "",        "actors": ["R3"]}    ← 异常
  ],
  "activeTaskList": ["stage_pm"],
  "variables.u_deptName": "默认部"          ← 异常：应该是 "FDEP协作组"
}
```

### Test 2: `u_fdp_pm` 的 todoList

```
DOING=[(92196795700227, 'stage_pm', ['R3'])], returned=[]
```

u_fdp_pm 看不到自己的 todo —— 因为 actors=['R3'] 是抽象角色，引擎未解析到 u_fdp_pm。

### Test 3: execute stage_pm（强行推进）

```json
{"code": 99999999, "msg": "[ValueError] 实例 state=20 不可执行任务（仅 DOING=10 可执行）"}
```

实例已被锁死，引擎拒绝执行。

### Test 4: doingList（引擎内部视角）

```
DOING instance 数: 0
DOING task 数: 0
by_node: {}
```

引擎自己也不认这个 instance 是 DOING 了 —— **状态彻底矛盾**：instance=DONE，task=DOING，doingList=空。

---

## 5. 问题清单

### 🔴 P0: W012 BUG-1 §110 重现

`stage_intake` 有 2 条出边（e1_approve → stage_pm + e1_reject → end_rejected），引擎遍历两条边后误把 end_rejected 当终点，instance 标记 DONE，但同时 stage_pm 也被激活为 DOING task。

**影响**：流程无法走完任何包含"分支 + end"的 task。

**修复**：必须用 fork/decision 节点分隔分支（参见 `ToT/README.md#5.3 Q5` 与 `ToT/flows/FDEP.json` openQuestions）。

### 🔴 P0: 角色解析断裂

`stage_pm.actors = ['R3']`（仅抽象角色 R3，未映射到 `u_fdp_pm`）。

引擎在 task 创建时**没有**查 SPI 把 `R3` 解析到 `fdep_rml` → `u_fdp_pm`，导致后续 task 无人可拾起。

**影响**：即使 W012 修复，流程走到 stage_pm 仍然卡死（除非把 FDEP.json 的 `assignee` 从 `R3` 改为 `fdep_rml` 等 SPI 角色名）。

### 🟡 P1: SPI dept 解析失败

`variables.u_deptName = "默认部"`（应为 "FDEP协作组"）。

引擎的 dept lookup 没走 `spi/dev/data.py` 路径解析 DFDEP，落到 default fallback。

**影响**：变量脏数据，但**不阻塞**流程（用户身份仍正确：`u_deptId=DFDEP`）。

### 🟢 已确认正确

- `stage_intake.actors = ['R3', 'u_fdp_pm']` —— startAndExecute 注入的 operator=u_fdp_pm **正确解析**（说明引擎在该路径下能识别 R3）
- `stage_intake.taskState=DONE` —— 第一 task 被正确执行
- deploy 流程正常，define_id=1
- 业务流在 start 之后立刻激活 stage_intake，符合预期

---

## 6. 决策建议（看结果再定）

| 候选 | 改动范围 | 工作量 | 效果 |
|------|----------|--------|------|
| **A. 修 W012 + 把 R3 改为 fdep_rml** | FDEP.json | 小 | 解决 P0，可能还有其他分支问题 |
| **B. A + 让 README §5 镜像同步** | FDEP.json + README | 中 | 一致性最好 |
| **C. 只修 W012，让角色解析问题作为已知** | FDEP.json | 小 | 流程跑通但任务卡死 |
| **D. 暂不修，把 FDEP.json 标为"概念验证不可执行"** | 文档 | 最小 | 保留 v0.5 作为设计稿 |

---

## 7. 关联

- `ToT/flows/FDEP.json` — 被测对象
- `ToT/README.md#5.3 Q5` — openQuestion 已预知驳回路径表达问题
- `ToT/sop/spi-verify.md` — 数据完整性 SOP（本次 PASSED）
- `ToT/mapping.md` — 抽象 R 角色 → SPI 角色映射（**未在引擎执行路径生效**）

## 8. 变更

| 时间 | 操作 |
|------|------|
| 2026-09-22 08:12 | 实跑首跑，W012 + 角色断裂两 P0 同时浮现，记录于本文件 |
