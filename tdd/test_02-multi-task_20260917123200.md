# 02-multi-task PG 后端覆盖测试

- **测试时间**：2026-09-17 12:32:00
- **后端**：PG
- **关联流程**：`./flows/02-multi-task.json`

## 1. 设计概览

```
start → apply(applicant) → task1(leader) → task2(manager) → task3(boss) → end
```

## 2. 部署

reset → save (name=multi-task) → design_id=1789614911959000 → deploy processDefineId=1789614912016000 ✅

## 3. 启动 + 执行

→ instanceId=91765669938180

执行 leader → manager → boss，全部 code:0

## 4. 校验

```
state=20 (DONE)
  apply  state=20 actorIds=['user1']
  task1  state=20 actorIds=['leader']
  task2  state=20 actorIds=['manager']
  task3  state=20 actorIds=['boss']
approvalRecord: apply(user1)→task1(leader)→task2(manager)→task3(boss)
```

## 5. AGENTS.md §10 自检

- [x] save + deploy code:0
- [x] startAndExecute 返回 processInstanceId
- [x] state==20
- [x] approvalRecord 顺序符合设计
- [x] 所有 action 在 §5.1 速查表内
- [x] 测试日志落盘
- [x] 环境重置

## 6. 复盘

无新发现。PG 后端 ID 仍是雪花 ID（19 位），与 01 一致。流程行为与 sqlite 后端完全一致。

## 7. 结论

✅ **PASS** — 02-multi-task PG 全流程符合设计。
