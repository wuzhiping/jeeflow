# 11-assignee-vars PG 后端覆盖测试

- **测试时间**：2026-09-17 12:52:00
- **后端**：PG
- **关联流程**：`./flows/11-assignee-vars.json`

## 1. 设计概览

```
start → apply(applicant) → task1(assignee="deptLeader") → task2(assignee="userA,userB" SEQUENTIAL) → end
```

## 2. 部署

reset → save → deploy pdid=1789615372746000 ✅

## 3. 启动 + 执行

→ instanceId=91766141713462

`deptLeader="userX"`, `userA="userA"`, `userB="userB"` 顶层传递。

执行 userX → userA → state=20

## 4. 校验

```
state=20 (DONE)
  apply  state=20 actorIds=['user1']
  task1  state=20 actorIds=['userX']             ← deptLeader 解析
  task2  state=20 actorIds=['userA', 'userB']    ← userA/userB 解析
```

## 5. AGENTS.md §10 自检

- [x] save + deploy code:0
- [x] startAndExecute 返回 processInstanceId
- [x] state==20
- [x] assignee 变量解析正确
- [x] 所有 action 在 §5.1 速查表内
- [x] 测试日志落盘
- [x] 环境重置

## 6. 复盘

无新发现。

## 7. 结论

✅ **PASS** — 11-assignee-vars PG 全流程符合设计。
