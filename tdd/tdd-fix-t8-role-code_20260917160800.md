# TDD FIX-T8: vendor/jeeflow/builtin.py TaskRoleAssigneeHandler 支持 properties.roleCode

- **时间**：2026-09-17 16:08:00
- **修复位置**：`vendor/jeeflow/builtin.py:135-149`
- **背景**（§68）：TaskRoleAssigneeHandler 仅以 `node.id` 查角色，Java 设计器用 `properties.roleCode` 显式指定角色时无法解析
- **服务**：main.py（PID 3482XXX）

## 1. 上游修复（vendor/jeeflow/builtin.py）

```python
class TaskRoleAssigneeHandler(IAssignmentHandler):
    """任务节点关联角色（FIX-T8 2026-09-17：优先 properties.roleCode，回落 node.id）
    - properties.roleCode 显式指定角色编码（Java 设计器规范字段）
    - 未配置时回落 node.id（保持向后兼容 §68 行为）
    """

    def __init__(self, org_prov: Optional[OrgUserProvider] = None):
        self.org_prov = org_prov

    async def assign(self, node, inst, operator) -> list[str]:
        if node is None or self.org_prov is None:
            return []
        # FIX-T8：优先 properties.roleCode，回落 node.id
        role_code = (node.properties or {}).get("roleCode") or node.id
        return await self.org_prov.find_by_role(role_code) or []
```

## 2. 测试结果

### Case A：properties.roleCode='leader'（node.id='review'）

设计：
```json
{
    "id": "review",
    "type": "snaker:task",
    "properties": {
        "assignmentHandler": "com.mldong.jeeflow.interceptor.impl.OrgUserAssignmentHandlers$TaskRoleAssigneeHandler",
        "roleCode": "leader",
        ...
    }
}
```

结果：
```
active: [('review', ['leader'])]   ✅
```

SPI role 'leader' 返回 ['leader']，properties.roleCode 正确解析。

### Case B：无 roleCode（回落 node.id）

设计：
```json
{
    "id": "finance",
    "type": "snaker:task",
    "properties": {
        "assignmentHandler": "com.mldong.jeeflow.interceptor.impl.OrgUserAssignmentHandlers$TaskRoleAssigneeHandler",
        "performType": 1,  // COUNTERSIGN
        "countersignType": "PARALLEL"
    }
}
```

结果：
```
active: [('finance', ['leader']), ('finance', ['manager'])]   ✅
```

无 roleCode → 用 node.id='finance' → SPI role 'finance' → ['leader', 'manager']，回落兼容。

## 3. 关键发现

1. **Java 设计器规范**：properties.roleCode 是规范的"节点关联角色"字段（比 node.id 更稳定）
2. **向后兼容**：无 roleCode 时用 node.id（原有行为不变）
3. **SPI 双轨制**：node.id='finance' 同时是 fallback；如 ID 重构可无缝迁移到 roleCode
4. **handler register FQCN**：必须用完整版 `com.mldong.jeeflow.interceptor.impl.OrgUserAssignmentHandlers$TaskRoleAssigneeHandler`（不是简化版），与 builtin.py:22 注册一致
5. **COUNTERSIGN 触发**：performType=1 自动生成多条 active task（['leader'] + ['manager']）

## 4. 关联修复

- 与 TDD-04 co_sign 流程一致（performType=1 + countersignType=PARALLEL + node.id='co_sign'）
- 后续可统一迁移所有 TaskRoleAssignee 节点从 node.id → properties.roleCode
