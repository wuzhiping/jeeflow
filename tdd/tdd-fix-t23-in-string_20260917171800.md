# FIX-T23 TDD 报告

## 目标
IN/NIN 操作符字符串逗号分隔兼容（设计器传 "1,2,3" 而非 list）

## 改动
- **vendor/jeeflow/memory.py:_match_conditions**（FIX-T23，2026-09-17）
  - IN 分支：字符串逗号分隔转 list
- **vendor/jeeflow/repository/ext.py:_build_ext_where**（FIX-T23，2026-09-17）
  - IN/NIN 分支：字符串逗号分隔转 list（PG 端）

## TDD 用例
| ID | 场景 | 期望 | 实际 |
|----|------|------|------|
| 1 | m_IN_id="9,11" page 设计 | rows=2, total=2 | ✅ rows=2 |
| 2 | m_NIN_id="9,11" page 设计 | rows=1, total=1（剩余 design） | ✅ rows=1 |
| 3 | m_IN_id=["9","11"] (list) | rows=2 | ✅ rows=2（list 兼容） |

## 端到端验证
- 8101 memory 后端：✅ IN 字符串过滤生效
- 8102 PG 后端：✅ list 形式（PG JdbcProcessExtRepository）

## 复盘
- 之前设计器传 IN 字符串时会被整体当成单值（"9,11"），filter 失效
- 现在兼容设计器常见调用方式
- 后续：可在 facade._parse_m_query 也加 IN list 自动转换（统一边界）
