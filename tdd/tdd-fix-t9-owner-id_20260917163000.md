# TDD FIX-T9: vendor/jeeflow/model.py+facade.py+engine.py+repository/base.py ProcessInstance.ownerId

- **时间**：2026-09-17 16:30:00
- **修复位置**：
  - `vendor/jeeflow/model.py:129` ProcessInstance.ownerId 字段
  - `vendor/jeeflow/model.py:392` InstanceRow.ownerId 字段
  - `vendor/jeeflow/model.py:311` CcInstanceRow.ownerId 字段
  - `vendor/jeeflow/engine.py:65` start_process_instance_by_id 提取 ownerId
  - `vendor/jeeflow/facade.py:157` _startAndExecute 兜底提取
  - `vendor/jeeflow/facade.py:131` _processInstance_detail 返回 ownerId
  - `vendor/jeeflow/facade.py:1474` _instance_row_to_dict 返回 ownerId
  - `vendor/jeeflow/repository/base.py:272-275` _INSTANCE_COLS 加 owner_id
  - `vendor/jeeflow/repository/base.py:283` find_instance_by_id 读 ownerId
  - `vendor/jeeflow/repository/base.py:300-307` save_instance 写 owner_id
  - `vendor/jeeflow/repository/base.py:311-318` update_instance 写 owner_id
  - `vendor/jeeflow/repository/base.py:533` page_instances cols 加 t.owner_id
  - `vendor/jeeflow/repository/base.py:713` _map_instance_row 取 r[7]
  - `vendor/jeeflow/repository/base.py:488` page_cc_instances cols 加 t.owner_id
  - `vendor/jeeflow/repository/base.py:499` _map_cc_row 取 r[7]
  - `vendor/jeeflow/repository/base.py:98-101` _INSTANCE_WHITELIST 加 t.owner_id
  - `vendor/jeeflow/repository/base.py:105` _CC_WHITELIST 加 t.owner_id
  - `vendor/jeeflow/memory.py:147-158` page_instances 写 ownerId
  - `vendor/jeeflow/memory.py:110` page_cc_instances 写 ownerId
  - `vendor/jeeflow/memory.py:427` _INSTANCE_FIELDS 加 t.owner_id
  - `docs/pg_schema.sql:27-29` 加 owner_id 列 + 索引
- **背景**（§66）：Java 设计器 ProcessInstance.ownerId 字段，Python 端 ProcessInstance 无该字段 → 待办列表"我发起的"按 ownerId 过滤无法实现
- **服务**：main.py（memory）+ main_pg.py（PG）双后端

## 1. ownerId 提取优先级

```python
owner_id = (
    str(args.get("ownerId", "") or "").strip()       # 1. 显式 ownerId
    or str(args.get("u_userId", "") or "").strip()   # 2. 发起时原始 u_userId（不被 user_prov 覆盖）
    or operator                                       # 3. operator
)
```

注意 `args.get("u_userId")` 而非 `vars_.get("u_userId")`：`_add_user_info` 会用 user_prov 覆盖 `u_userId`，所以必须在覆盖前提取。

## 2. 测试结果

### Memory 后端（main.py）

```
Case A (operator=alice, u_userId=alice):   ownerId='alice'  ✅ (operator fallback)
Case B (ownerId=bob, operator=alice):     ownerId='bob'    ✅ (显式 ownerId)
Case C (u_userId=carol, operator=alice):   ownerId='carol'  ✅ (原始 u_userId)
```

### PG 后端（main_pg.py）

```
detail: A='alice', B='bob', C='carol'  ✅
page_instances: A='alice', B='bob', C='carol'  ✅
DB 直接查 wf_process_instance.owner_id: alice/bob/carol  ✅
m_EQ_ownerId=bob 过滤: 只返回 bob  ✅
```

## 3. 关键发现

1. **engine + facade 双轨制**：
   - engine.start_process_instance_by_id 主提取（_add_user_info 之后）
   - facade._startAndExecute 兜底（防御 engine 跳过）
   - args.u_userId 而非 vars_.u_userId（必须在 _add_user_info 覆盖前提取）
2. **字段顺序错位**：
   - 第一次实现时 r[16] 越界 → 实际 r[7] 是 owner_id（cols 加在 expire_time 之前）
   - _map_instance_row 和 _map_cc_row 都需修正
3. **双后端覆盖**：
   - PG: ALTER TABLE 加 owner_id 列 + 索引 + 白名单 + 字段映射
   - Memory: InstanceRow 字段 + 字段映射
4. **PG ownerId 过滤**：m_EQ_ownerId 语法工作（m_ 前缀 + 表别名 + camelCase）

## 4. PG Schema 变更

```sql
-- docs/pg_schema.sql:27
ALTER TABLE wf_process_instance ADD COLUMN owner_id VARCHAR(64);
CREATE INDEX idx_wf_process_instance_owner ON wf_process_instance(owner_id);
```

## 5. 后续建议

- §66 已知问题标记：FIX-T9 修复
- §74 vendor 工作流：本次跨 facade/engine/repository/model 多文件改动
- AGENTS.md §8 调试：可读 vendor/jeeflow/*.py 排查字段映射问题

## 6. 测试报告

- vendor 上游修复：✅
- memory + PG 双后端一致：✅
- ownerId 优先级正确：✅
- m_ 前缀过滤语法：✅
- DB 落库正确：✅
