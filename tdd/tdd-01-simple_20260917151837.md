# TDD 01: 简单审批流程 simple（PASS）

- **时间**：2026-09-17 15:18:37
- **流程定义**：`./flows/01-simple.json`
- **服务**：main.py（PID 3472967）

## 1. 流程图

```mermaid
flowchart LR
    A([开始]) --> B[apply 发起申请]
    B --> C[task1 上级审批]
    C --> D([结束])
```

| 节点 | 类型 | assignee |
|---|---|---|
| start | snaker:start | — |
| apply | snaker:task | applicant → user1 |
| task1 | snaker:task | leader |
| end | snaker:end | — |

## 2. 测试步骤

| # | 操作 | 结果 |
|---|---|---|
| 1 | `/api/reset` | OK（设计器清空，种子重载） |
| 2 | `processDesign/save` | design_id=9 |
| 3 | `processDesign/deploy` | processDefineId=113 |
| 4 | `startAndExecute` (user1, u_userId, u_realName, u_deptId) | inst=91780627303615, apply DOING |
| 5 | `processTask/execute submitType=1` (user1) | task1 DOING actor=leader |
| 6 | `processTask/execute submitType=1` (leader) | **state=20** |

## 3. 测试结果

| task | state | actors |
|---|---|---|
| apply | 20 | ['user1'] |
| task1 | 20 | ['leader'] |

实例终态 state=20，符合预期。

## 4. 复盘

### 流程设计

- 3 节点 2 边，线性最简结构
- assignee='applicant' → engine 替换为 inst.operator=user1
- assignee='leader' → 字面量，查 SPI DEMO_USERS.json userId=leader

### 关键观察

1. **apply 完成 actor=user1**：因 operator=user1，applicant→user1 替换成功
2. **task1 完成 actor=leader**：assignee 字面量 + `_resolve_actors` 落字面 actor
3. **无异常、无 warning**：FIX-T2 handler 日志未触发（无 assignmentHandler）
4. **2 秒内完成**：3 个 API 调用 + 2 次 sleep 0.5

### 与 AGENTS.md 对齐

- §5.4 启动实例：✅ processDefineId + operator + assignees + variables 顶层
- §5.5 task execute：✅ processTaskId + submitType + operator
- §3.3 节点 id 命名：apply/task1 是合法 snake_case

### 文档改进建议

无新发现。简单流程已记录于 `flows/01-simple.json`，无需 docs 增量。
