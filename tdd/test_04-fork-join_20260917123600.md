# 04-fork-join PG 后端覆盖测试

- **测试时间**：2026-09-17 12:36:00
- **后端**：PG
- **关联流程**：`./flows/04-fork-join.json`

## 1. 设计概览

```
start → apply(applicant) → fork1 → {taskA(userA) | taskB(userB)} → join1 → end
```

## 2. 部署

reset → save → deploy pdid=1789614968224000 ✅

## 3. 启动 + 执行

→ instanceId=91765727481873

并行 taskA(userA) + taskB(userB) DOING，执行后 → state=20

## 4. 校验

```
state=20 (DONE)
  apply  state=20 actorIds=['user1']
  taskA  state=20 actorIds=['userA']
  taskB  state=20 actorIds=['userB']
```

## 5. AGENTS.md §10 自检

- [x] save + deploy code:0
- [x] startAndExecute 返回 processInstanceId
- [x] state==20
- [x] fork-join 并行+汇合正确
- [x] 所有 action 在 §5.1 速查表内
- [x] 测试日志落盘
- [x] 环境重置

## 6. 复盘

无新发现。PG 后端 fork-join 行为与 sqlite 一致。

## 7. 结论

✅ **PASS** — 04-fork-join PG 全流程符合设计。
