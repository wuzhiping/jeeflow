# 11-assignee-vars 覆盖测试

- **测试时间**：2026-09-17 12:12:00
- **测试类型**：按 docs/AGENTS.md §5 完整流程
- **关联流程**：`./flows/11-assignee-vars.json`

## 1. 设计概览

```
节点：start → apply(applicant) → task1(assignee="deptLeader") → task2(assignee="userA,userB" SEQUENTIAL) → end
关键：assignee 字面量为变量名，引擎从 args 顶层查找替换
```

## 2. 部署

reset → save (name=assignee-vars) → deploy → processDefineId=113 ✅

## 3. 启动 + 执行

```bash
curl -X POST /wf/processInstance/startAndExecute -d '{
  "processDefineId": 113, "operator":"user1",
  "assignees":{"apply":"user1"},
  "variables":{"submitType":0, "f_applyUser":"user1", "f_title":"reg-11a", "u_userId":"user1", "u_realName":"用户1"},
  "deptLeader":"userX", "userA":"userA", "userB":"userB"   ← 顶层
}'
```

→ instanceId=91765146159954

启动后 task1 解析为 `actorIds=['userX']`（assignee 字符串 "deptLeader" → 变量 deptLeader="userX"）

执行 userX → userA（task2 SEQUENTIAL：userA → userB）

## 4. 校验

### 4.1 detail

```
state=20 (DONE)  active=0
  apply  state=20 actorIds=['user1']
  task1  state=20 actorIds=['userX']                    ← deptLeader 变量解析
  task2  state=20 actorIds=['userA', 'userB']           ← userA/userB 变量解析（SEQUENTIAL）
```

### 4.2 approvalRecord

```
apply  operator=user1
task1  operator=userX
task2  operator=userA
```

✅ assignee 变量解析行为符合设计：
- 单变量 task1：`assignee="deptLeader"` → `vars.deptLeader="userX"` → 解析为 userX
- 多变量 task2：`assignee="userA,userB"` → 各自解析

## 5. AGENTS.md §10 自检

- [x] save + deploy code:0
- [x] startAndExecute 返回 processInstanceId
- [x] 走完所有 task，state==20
- [x] approvalRecord 节点顺序符合设计
- [x] assignee 变量解析正确（task1=userX, task2=userA/userB）
- [x] 所有 action 在 §5.1 速查表内
- [x] 测试日志落盘
- [x] 环境重置

## 6. 复盘

无新发现。assignee 变量解析行为符合 docs/flow.md §6：
- 单变量：`assignee="<varName>"` → `args[varName]` → actorId
- 多变量：`assignee="<v1>,<v2>"` → 各自分别解析
- 变量**必须顶层**（与 Issue D 同源）

## 7. 结论

✅ **PASS** — 11-assignee-vars 全流程符合设计，变量解析正确。
