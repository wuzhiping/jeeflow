# FIX-T6: candidateUsers 不解析 actor 修复测试报告

- **测试时间**：2026-09-17 14:26:00（TS=20260917122600）
- **修复对象**：错误 6 — candidateUsers/candidateGroups 不解析 actor
- **JSON 定义**：
  - Case A 错误：`./tdd/fix-candidate-only_20260917122600.json`（仅 candidate）
  - Case B 修正：`./tdd/fix-candidate-with-assignee_20260917122600.json`（assignee + candidate）
- **状态**：✅ 检测 + 修复对比成功

## 1. 测试设计

| Case | assignee | candidateUsers | candidateGroups | 期望 |
|---|---|---|---|---|
| A 错误 | 无 | userB,userC | leader | 卡死 |
| B 修正 | userB,userC | userB,userC | leader | PASS |

## 2. 测试结果

### Case A：仅 candidate
- `detail`: state=10 active=0（review_only_candidate task 未创建）
- ✅ 错误检测：candidate 不解析 actor

### Case B：assignee + candidate
- `detail`: state=10 active=1 (review_only_candidate actors=['userB', 'userC'])
- userB execute → state=20 DONE
- ✅ 修复有效：assignee 解析 actor + candidate 仍配置（用于候选页查询）

## 3. 关键说明

**`engine.py:415-451 _resolve_actors` 仅识别**：
1. `vars_[KEY_NEXT_NODE_OPERATOR]`（动态指定）
2. `node.properties.assignee`（显式，逗号分隔多 actor）
3. `node.properties.assignmentHandler`（注册 handler）

**`candidateUsers/candidateGroups` 仅作用于 `processTask/candidatePage`**（`facade.py:977-984`），不影响 actor 解析。

## 4. candidate vs actor 概念

| 维度 | candidateUsers/Groups | assignee |
|---|---|---|
| 作用 | `candidatePage` 候选查询 | task actorIds |
| 多值 | 字符串列表 | 字符串列表（逗号分隔） |
| task 执行 | **无影响** | **直接影响**（任一可办） |
| 配合 | 可与 assignee 共存 | 必须独立或与 candidate 共存 |

## 5. TDD 自检清单

- [ ] 每个 task 节点必须配 assignee 或 assignmentHandler（candidate 不算）
- [ ] candidateUsers/Groups 用于候选查询（如"我的待办"列表），不影响执行
- [ ] 多 actor 模式：`assignee="userA,userB"` 任一可办
- [ ] 会签多 actor：再加 `performType=1, countersignType`

## 6. 关联

- `./bdd/bdd-performance-review_20260917115800.md` Task 14（首次发现）
- `./bdd/fix-20260917122008.md` 错误 6
- `./docs/known-issues.md §22` candidatePage 透传（相关）
- `./docs/flow.md §3.3` candidate 字段说明
