# TDD FIX-T11+T12: vendor/jeeflow/repository/base.py stats 兜底（PG 兼容）

- **时间**：2026-09-17 16:48:00
- **修复位置**：
  - `vendor/jeeflow/repository/base.py:stats_avg_completed_duration_seconds` 加 timedelta 兜底（FIX-T11）
  - `vendor/jeeflow/repository/base.py:stats_completed_task_aggregate` perform_type 字面量改 '1'（FIX-T12）
- **去掉补丁**：main_pg.py `_fixed_stats_avg_dur` + `_fixed_stats_task_agg` + `import functools`
- **服务**：main_pg.py（PG）+ main.py（memory）双后端

## 1. FIX-T11 vendor 上游修复

```python
async def stats_avg_completed_duration_seconds(self, start=None, end=None) -> int:
    sql = ("SELECT AVG(ts.max_finish - i.create_time) FROM wf_process_instance i ...")
    ...
    if not r or r[0] is None:
        return 0
    # FIX-T11 (2026-09-17)：PG JDBC 返回 timedelta 而非 int（avg(timestamp-timestamp) → INTERVAL）
    # SQLAlchemy/asyncpg/psycopg 都把 timestamp-timestamp 解码为 datetime.timedelta；
    # SQLite/MySQL 直接返回 int 秒数。统一兼容：timedelta 转秒，其他原样 int。
    v = r[0]
    if hasattr(v, "total_seconds"):
        return int(v.total_seconds())
    return int(v)
```

## 2. FIX-T12 vendor 上游修复

```python
async def stats_completed_task_aggregate(self) -> tuple[int, int, int, int]:
    # FIX-T12 (2026-09-17)：perform_type 字面量兼容 PG VARCHAR 列
    sql = ("SELECT COUNT(*), "
           "SUM(CASE WHEN perform_type = '1' THEN 1 ELSE 0 END), "  # 原 = 1 改 = '1'
           ...)
```

## 3. 测试结果

### PG 后端（main_pg.py）

```
processInstance/stats/overview: ✅
processInstance/stats/group dim=state: ✅ (state=10/20)
processInstance/stats/trend: ✅
```

### Memory 后端（main.py）

```
processInstance/stats/overview: ✅ (completed=9)
```

## 4. 关键发现

1. **vendor 自动适配**：SQLite/MySQL 仍走 int(r[0]) 路径（r[0] 无 total_seconds）
2. **PG INTERVAL 自动转秒**：timedelta.total_seconds() 兜底
3. **perform_type 字面量**：字符串 '1' 兼容 PG VARCHAR + SQLite 整数列
4. **去 main_pg.py 兜底**：减少 monkey patch 数量（lifespan 启动更快）

## 5. 测试报告

- vendor 上游修复：✅
- memory + PG 双后端一致：✅
- 3 个 stats action 全部通过：✅
- 去掉 main_pg.py 2 处 monkey patch + functools import：✅
