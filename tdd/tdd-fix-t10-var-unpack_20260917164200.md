# TDD FIX-T10: vendor/jeeflow/facade.py _startAndExecute 业务 variables 嵌套解包

- **时间**：2026-09-17 16:42:00
- **修复位置**：`vendor/jeeflow/facade.py:_startAndExecute` (FIX-T10 段)
- **背景**（Issue D）：调用方传 `{variables: {amount: 5000}}` 时 amount 被嵌入到 `inst.variables.variables.amount`，decision expr `vars_.get("amount")` 永远拿不到 → expr 永远 False → fallback 到 edges[0]
- **去掉补丁**：main.py + main_pg.py `_safe_flow` 中嵌套解包部分
- **服务**：main.py（memory）+ main_pg.py（PG）双后端

## 1. 上游修复（vendor/jeeflow/facade.py）

```python
async def _startAndExecute(self, args: dict) -> dict:
    define_id = self._to_int(args.get("processDefineId"))
    if not define_id:
        raise ValueError("processDefineId 缺失或非法")
    operator = str(args.get("operator", "user1"))
    # FIX-T10 (2026-09-17)：business variables 嵌套解包（§Issue D 上游修复）
    nested = args.get("variables")
    if isinstance(nested, dict):
        for k, val in nested.items():
            args.setdefault(k, val)  # 顶层已有 key 不覆盖
    flow_args = {k: v for k, v in args.items() if k not in ("processDefineId", "operator")}
    ...
```

## 2. 测试结果

### PG 后端（main_pg.py）

`flows/03-decision-expr.json` 流程，decision expr `amount > 1000`：

```
amount=5000 (nested): variables.amount=5000  ✅ active=[('task1', ['leader'])]
amount=500  (nested): variables.amount=500   ✅ active=[('task1', ['leader'])]
```

### Memory 后端（main.py）

```
amount=5000 (nested): variables.amount=5000  ✅
amount=500  (nested): variables.amount=500   ✅
```

## 3. 关键发现

1. **vendor 上游修复**：去掉 main.py + main_pg.py `_safe_flow` 中"嵌套 variables 展开"部分
2. **setdefault 而非赋值**：保留顶层已有 key（避免覆盖 operator/ownerId 等）
3. **decision expr 修复**：`vars_.get("amount")` 能直接读 nested 展开后的顶层 key
4. **main_pg.py `_safe_flow` 保留**：因为还有 Issue A/B（surrogate bool + ordinal 映射）需要

## 4. 测试报告

- vendor 上游修复：✅
- memory + PG 双后端一致：✅
- decision expr amount 解包正确：✅
- 去掉 main.py + main_pg.py monkey patch 嵌套解包部分：✅
