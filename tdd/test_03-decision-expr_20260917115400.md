# 03-decision-expr 覆盖测试（按 AGENTS.md §5 全流程）

- **测试时间**：2026-09-17 11:54:00
- **测试类型**：按 docs/AGENTS.md §5 完整流程（两分支）
- **关联流程**：`./flows/03-decision-expr.json`

## 1. 设计概览

```
节点：start → apply(applicant) → task1(leader) → decision1 → {task2(manager) | task3(director)} → end
边 expr:
  e3 decision1→task2 expr='amount > 1000'
  e4 decision1→task3 expr='amount <= 1000'
```

## 2. 部署（§5.3）

| 步骤 | 结果 |
|---|---|
| reset | code:0 |
| save (name=decision-expr) | code:0 |
| deploy | code:0, processDefineId=113 |

## 3. 启动 + 执行（§5.4-§5.5）

### Test A: amount=500 (≤1000 → task3 director)

```bash
curl -X POST /wf/processInstance/startAndExecute -d '{
  "processDefineId": 113, "operator":"user1",
  "assignees":{"apply":"user1"},
  "variables":{"submitType":0,"f_applyUser":"user1","f_purpose":"low",
               "u_userId":"user1","u_realName":"用户1"},
  "amount": 500    ← 顶层
}'
```

→ instanceId=91764761337806

执行：task1(leader) → task3(director)

### Test B: amount=2000 (>1000 → task2 manager)

→ instanceId=91764762474450

执行：task1(leader) → task2(manager)

## 4. 校验（§5.6）

### 4.1 Test A detail

```
state=20 (DONE)  active=0
  apply  state=20 actorIds=['user1']
  task1  state=20 actorIds=['leader']
  task3  state=20 actorIds=['director']   ← ✅ decision 走 amount<=1000
```

approvalRecord: apply(user1) → task1(leader) → task3(director)

### 4.2 Test B detail

```
state=20 (DONE)  active=0
  apply  state=20 actorIds=['user1']
  task1  state=20 actorIds=['leader']
  task2  state=20 actorIds=['manager']    ← ✅ decision 走 amount>1000
```

## 5. AGENTS.md §10 自检

- [x] §5.3 save + deploy code:0
- [x] §5.4 startAndExecute 返回 processInstanceId（2 次）
- [x] §5.5 走完所有 task，state==20（两条分支都 DONE）
- [x] §5.6 approvalRecord 节点顺序符合设计（apply→task1→task2/3）
- [x] §5.6 decision 分支正确（amount=500→task3, amount=2000→task2）
- [x] 所有 action 在 §5.1 速查表内
- [x] 测试日志落盘
- [x] 环境重置

## 6. 复盘

**关键确认**：
- `amount` 变量**必须顶层传递**（不在 variables 嵌套），decision expr `vars_.get("amount")` 才能取到值
- 走 Issue D 修复后的 `main.py` monkey-patch `facade.flow`：startAndExecute 前把 args["variables"] 字典展开到 args 顶层
- 即便如此，`amount` 仍走**顶层**而非嵌套（设计如此；变量解包只针对 `variables` 子字典）

无新发现，跳过 docs 改动。

## 7. 结论

✅ **PASS** — 03-decision-expr 两个分支均符合设计，state=20 DONE。
