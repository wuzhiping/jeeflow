# 04-fork-join 覆盖测试（按 AGENTS.md §5 全流程）

- **测试时间**：2026-09-17 11:56:00
- **测试类型**：按 docs/AGENTS.md §5 完整流程
- **关联流程**：`./flows/04-fork-join.json`

## 1. 设计概览

```
节点：start → apply(applicant) → fork1 → {taskA(userA) | taskB(userB)} → join1 → end
边：start→apply, apply→fork1, fork1→taskA, fork1→taskB, taskA→join1, taskB→join1, join1→end
字段权限：taskA/taskB 上 PERMISSION_f_title=1
```

## 2. 部署（§5.3）

reset → save (name=fork-join) → deploy → processDefineId=113 ✅

## 3. 启动 + 执行（§5.4-§5.5）

→ instanceId=91764792657973

启动后并行 active=2：
- taskA(userA) DOING
- taskB(userB) DOING

执行：taskA by userA → code:0；taskB by userB → code:0

## 4. 校验（§5.6）

### 4.1 detail

```
state=20 (DONE)  active=0
  apply  state=20 actorIds=['user1']
  taskA  state=20 actorIds=['userA']
  taskB  state=20 actorIds=['userB']
```

### 4.2 approvalRecord

```
apply  operator=user1
taskA  operator=userA
taskB  operator=userB
```

✅ 并行节点同时推进、join 后 state=20

## 5. AGENTS.md §10 自检

- [x] §5.3 save + deploy code:0
- [x] §5.4 startAndExecute 返回 processInstanceId
- [x] §5.5 走完所有 task，state==20
- [x] §5.6 approvalRecord 节点顺序符合设计
- [x] §5.6 fork-join 行为正确（并行执行、汇合后 DONE）
- [x] 所有 action 在 §5.1 速查表内
- [x] 测试日志落盘
- [x] 环境重置

## 6. 复盘

无新发现。Fork-join 行为符合设计：apply → fork → 并行 taskA/taskB → join → end。**字段权限**（taskA/taskB 的 PERMISSION_f_title）未覆盖。

## 7. 结论

✅ **PASS** — 04-fork-join 全流程符合设计。
