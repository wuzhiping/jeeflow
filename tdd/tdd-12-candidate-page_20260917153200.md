# TDD 12: 候选人分页 candidate-page（PASS）

- **时间**：2026-09-17 15:32:00
- **流程定义**：`./flows/12-candidate-page.json`

## 1. 流程图

```mermaid
flowchart LR
    A([开始]) --> B[apply]
    B --> C[review leader]
    C --> D([结束])
```

3 节点流程，演示 candidatePage API。

## 2. 测试步骤

| # | 操作 | 结果 |
|---|---|---|
| 1 | reset + save + deploy | PDID=113 |
| 2 | startAndExecute | inst, apply auto-done |
| 3 | candidatePage(operator=空) | **total=8** 全量演示用户 |
| 4 | review.execute(leader) | **state=20** ✅ |

## 3. candidatePage 实测

```
total: 8
  user1 张三 D01
  userA 孙倩 D01
  userB 周明 D01
  userC 吴婷 D01
  leader 李四 D01
  manager ...
  director ...
  boss ...
```

## 4. 关键发现

1. **candidatePage 是 facade API**：基于 processTaskId
2. **无 operator 参数 → 全量**：8 个演示用户全部返回
3. **API 行为**：与 §37 一致（按 processTaskId 查后继节点的 candidates）
4. **流程逻辑无关**：candidatePage 只是查询接口，不影响流程推进

## 5. 文档改进

无新发现。candidatePage 已记录于 `docs/known-issues.md §37` + `docs/flow.md §6`。

## 6. 测试报告

- 流程 12 candidate-page：✅ 全通过
- API + 流程逻辑都正常
