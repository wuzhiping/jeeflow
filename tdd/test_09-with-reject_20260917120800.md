# 09-with-reject 覆盖测试

- **测试时间**：2026-09-17 12:08:00
- **测试类型**：按 docs/AGENTS.md §5 完整流程（双场景）
- **关联流程**：`./flows/09-with-reject.json`

## 1. 设计概览

```
节点：start → apply(applicant) → task1(leader) → task2(manager) → end
边 expr (reject 路径):
  task1 → reject 边 (submitType=2)
  task2 → reject 边 (submitType=2)
```

## 2. 部署

reset → save (name=with-reject) → deploy → processDefineId=113 ✅

## 3. 启动 + 执行（双场景）

### Test A: happy path (submitType=0)

→ instanceId=91765075009161

执行 leader → manager 全部 code:0

### Test B: task1 reject (submitType=2)

→ instanceId=91765076213389

执行 leader submitType=2 → code:0

## 4. 校验

### 4.1 Test A detail

```
state=20 (DONE)  active=0
  apply  state=20
  task1  state=20
  task2  state=20
```

approvalRecord: apply(user1) → task1(leader) → task2(manager) ✅

### 4.2 Test B detail

```
state=45 (REJECT)  active=0
  apply  state=20
  task1  state=20    ← reject 时 task1 也标 DONE
```

✅ submitType=2 走 `execute_and_jump_to_end` → 实例级 REJECT (state=45)，task2 未创建

## 5. AGENTS.md §10 自检

- [x] save + deploy code:0
- [x] startAndExecute 返回 processInstanceId（2 次）
- [x] A: state==20；B: state==45
- [x] approvalRecord 节点顺序符合设计
- [x] submitType=2 触发 REJECT 路径
- [x] 所有 action 在 §5.1 速查表内
- [x] 测试日志落盘
- [x] 环境重置

## 6. 复盘

无新发现。SubmitType 行为与 `docs/state.md §2` 一致：
- `submitType=2 (REJECT)` → 实例级终止 state=45
- `submitType=0 (APPLY)` → 同意流转

task1 reject 时 `task1 state=20` —— engine 完成 task1（标记 DONE）后触发 `execute_and_jump_to_end`，实例级 REJECT。这是已知行为。

## 7. 结论

✅ **PASS** — 09-with-reject 双场景符合设计（happy path DONE, reject path REJECT）。
