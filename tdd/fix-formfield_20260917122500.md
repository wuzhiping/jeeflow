# FIX-T5: FormField handler 字段名错配修复测试报告

- **测试时间**：2026-09-17 14:25:00（TS=20260917122500）
- **修复对象**：错误 5 — FormField handler 字段名错配
- **JSON 定义**：
  - Case A 错误：`./tdd/fix-formfield-bad_20260917122500.json`（`f_pm_reviewer`，节点 `pm_review`）
  - Case B 修正：`./tdd/fix-formfield-good_20260917122500.json`（`f_pm_review`，匹配节点 id）
- **状态**：✅ 检测 + 修复对比成功

## 1. 测试设计

| Case | 节点 id | 字段名 | 期望 | 实际 |
|---|---|---|---|---|
| A 错配 | pm_review | f_pm_reviewer | 卡死 | state=10 active=0 ✓ |
| B 修正 | pm_review | f_pm_review | PASS | state=20 DONE ✓ |

## 2. 测试结果

### Case A：字段名错配
- `detail`: state=10 active=0（pm_review task 未创建）
- stderr 日志：
  ```
  [FIX-T2 WARN] handler 'com.mldong.jeeflow.interceptor.impl.FormFieldAssigneeHandler' returned empty actors for node_id='pm_review' (process=113); check SPI role_code
  ```
- ⚠️ warning 信息说 "check SPI role_code" 但实际是字段名错配 — warning 信息对 FormField 不准确

### Case B：字段名修正
- `detail`: state=10 active=1 (pm_review actors=[user1]) → user1 execute → state=20
- ✅ 流程 PASS

## 3. 关键发现 ⚠️ FIX-T2 warning 信息对 FormField 不准确

**当前 warning**：`returned empty actors ... check SPI role_code`
- 对 TaskRole handler 准确
- 对 FormField handler **不准确**（字段名错配也走这条 warning）

**改进建议**（后续 FIX-T2c）：
```python
# 区分 handler 类型
if "FormField" in handler_name:
    msg += f"; check field name 'f_{node.id}' in variables"
elif "TaskRole" in handler_name:
    msg += f"; check SPI role_code '{node.id}'"
```

**TDD 排查方法**：看到 `check SPI role_code` 后**优先检查**：
- TaskRole handler → SPI `DEMO_ROLE_TO_USERS.json` 含 `<node.id>` 键
- FormField handler → startAndExecute variables 含 `f_<node.id>` 字段
- DeptLeader handler → SPI `DEMO_DEPT_LEADERS.json` 含 `<u_deptId>` 键

## 4. FormField handler 字段名约束

**`builtin.py:55` `_find_field_value(variables, field_name)`**：
```python
def _find_field_value(self, variables, field_name):
    if "f_" + field_name in variables:
        return variables["f_" + field_name]
    # 回落：去数字后缀、裸 key 等
```

**字段名规则**：`f_<node.id>`（handler 强制），可能回落：去数字后缀（`f_node_1` → `f_node`）、裸 key。

## 5. TDD 自检清单

- [ ] FormField handler 节点的 `field` 字段名 = `f_<node.id>`
- [ ] startAndExecute 顶层 `variables` 必须含该字段
- [ ] 字段值类型正确（userId 字符串）
- [ ] 走完所有 task 后 state=20

## 6. 关联

- `./bdd/bdd-project-init_20260917114047.md` Task 5（首次发现）
- `./bdd/fix-20260917122008.md` 错误 5
- `./docs/known-issues.md §24` 字段名约束
- `./docs/known-issues.md §29` FIX-T2 warning（待改进：区分 handler 类型）
