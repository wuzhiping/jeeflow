# 08-custom-node 覆盖测试

- **测试时间**：2026-09-17 12:06:00
- **测试类型**：按 docs/AGENTS.md §5 完整流程
- **关联流程**：`./flows/08-custom-node.json`
- **关联已知问题**：`./docs/known-issues.md §16`（自定义节点反射未实现）

## 1. 设计概览

```json
{
  "nodes": [
    {"id": "start", "type": "snaker:start"},
    {"id": "apply", "type": "snaker:task", "properties": {"assignee": "applicant"}},
    {"id": "custom1", "type": "snaker:custom",
     "properties": {
       "clazz": "com.example.MyHandler",
       "methodName": "doSomething",
       "args": {"x": 1},
       "val": "result"
     }},
    {"id": "end", "type": "snaker:end"}
  ],
  "edges": [
    {"sourceNodeId":"start","targetNodeId":"apply"},
    {"sourceNodeId":"apply","targetNodeId":"custom1"},
    {"sourceNodeId":"custom1","targetNodeId":"end"}
  ]
}
```

## 2. 部署

reset → save (name=custom-node) → deploy → processDefineId=113 ✅

## 3. 启动 + 执行

→ instanceId=91764991561256

启动后 detail：
```
state=10 (DOING)  active=0
tasks: [apply state=20]
```

apply 完成后，**custom1 节点未创建 task**，流程卡在 state=10 永久 DOING。

## 4. 校验

| 期望 | 实测 | 结论 |
|---|---|---|
| state=20 DONE | state=10 DOING | ❌ |
| tasks 含 custom1 | tasks 仅 apply | ❌ |

## 5. AGENTS.md §10 自检

- [x] save + deploy code:0
- [x] startAndExecute 返回 processInstanceId
- [ ] **走完所有 task，state==20** ❌
- [ ] approvalRecord 节点顺序符合设计 ❌
- [ ] 所有 action 在 §5.1 速查表内 ✅
- [x] 测试日志落盘
- [x] 环境重置

## 6. 根因（已知 §16）

`engine._create_task` 共用 `TYPE_TASK/CUSTOM`，**无 clazz/methodName 反射实现**。custom 节点解析时未触发 task 创建，导致流程卡死。

具体引擎代码路径见 `./docs/known-issues.md §16`。

## 7. 复盘与 docs

### 7.1 不新增 docs 改动

本问题已在 `./docs/known-issues.md §16` 完整记录（自定义节点反射未实现）。本次实测与 §16 描述一致（state=10 DOING, custom1 未创建, apply 完成后流程卡死）。

### 7.2 §16 现状复核

§16 已经包含：
- 现象描述
- 引擎代码路径
- 设计层修复建议（避免使用 snaker:custom）
- engine 层修复建议

**本轮无需重复记录**。

## 8. 结论

❌ **FAIL** — 08-custom-node 流程卡死在 custom1 节点，state=10 DOING 永远不流转。
**原因**：已知 §16，引擎层 custom 节点反射未实现，非配置/数据可修复。
