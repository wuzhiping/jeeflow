# 10-mixed-mode 覆盖测试

- **测试时间**：2026-09-17 12:10:00
- **测试类型**：按 docs/AGENTS.md §5 完整流程
- **关联流程**：`./flows/10-mixed-mode.json`

## 1. 设计概览

```
节点：start → apply(applicant) → fork1 → {task2(checker) | task3(reviewer)} → join1 → decision1 → {task4(boss) | end}
决策：finalAmount > 5000 → task4(boss)，否则 → end
顶层 type=approval，preInterceptors/postInterceptors 顶层字段
```

## 2. 部署

reset → save (name=mixed-mode) → deploy → processDefineId=113 ✅

## 3. 启动 + 执行

→ instanceId=91765110418159

启动后并行 active=2：
- task2(checker) DOING
- task3(reviewer) DOING

执行 checker → reviewer，全部 code:0

`finalAmount=3000`（**顶层**）→ decision 评估 → finalAmount <= 5000 → end

## 4. 校验

### 4.1 detail

```
state=20 (DONE)  active=0
  apply  state=20 actorIds=['user1']
  task2  state=20 actorIds=['checker']
  task3  state=20 actorIds=['reviewer']
  ← task4 未创建（决策走 end）
```

### 4.2 approvalRecord

```
apply  operator=user1
task2  operator=checker
task3  operator=reviewer
```

✅ decision 正确走 end 分支

## 5. AGENTS.md §10 自检

- [x] save + deploy code:0
- [x] startAndExecute 返回 processInstanceId
- [x] 走完所有 task，state==20
- [x] approvalRecord 节点顺序符合设计
- [x] decision expr `finalAmount > 5000` 正确路由（finalAmount=3000 → end）
- [x] finalAmount **顶层传递**（Issue D 修复后）
- [x] 所有 action 在 §5.1 速查表内
- [x] 测试日志落盘
- [x] 环境重置

## 6. 复盘

无新发现。10-mixed-mode 复合场景（fork-join + decision + 顶层变量）行为符合设计：
- `finalAmount` 必须在**顶层**（不在 `variables` 嵌套），否则决策 expr 评估失败走 fallback 首边（Issue D 已修复）
- task4 仅在 finalAmount > 5000 时创建（本次未触发）

## 7. 结论

✅ **PASS** — 10-mixed-mode 全流程符合设计，决策正确路由到 end。
