# BDD Regression Test: 01-simple

- **时间**：2026-09-17 17:35
- **流程**：`./flows/01-simple.json`（申请人→上级→结束）
- **服务**：main.py (8101) + main_pg.py (8102)

## 1. 测试结果

| 步骤 | 期望 | 实际 |
|------|------|------|
| 部署 (pdid=157) | 成功 | ✅ |
| 启动实例 | actor=leader | ✅ actor='leader' |
| leader execute | 状态 20 | ✅ state=20 |
| approvalRecord | [apply, task1] | ✅ |
| Memory 后端 | 全过 | ✅ |
| PG 后端 | 全过 | ✅（同步验证） |

## 2. 端到端 curl 关键命令

```bash
# 部署
curl -X POST http://localhost:8101/wf/processDesign/save -d '{"name":"simple",...}'
curl -X POST http://localhost:8101/wf/processDesign/deploy -d '{"id":...}'

# 启动
curl -X POST http://localhost:8101/wf/processInstance/startAndExecute -d '{
  "processDefineId":157,"operator":"user1","title":"测试",
  "assignees":{"apply":"user1"},
  "variables":{"submitType":0,"u_userId":"user1","u_realName":"用户1"}}'

# 执行（leader）
curl -X POST http://localhost:8101/wf/processTask/todoList -d '{"operator":"leader"}'
curl -X POST http://localhost:8101/wf/processTask/execute -d '{"processTaskId":...,"submitType":0,"operator":"leader"}'

# 校验
curl -X POST http://localhost:8101/wf/processInstance/detail -d '{"id":...}'
curl -X POST http://localhost:8101/wf/processInstance/approvalRecord -d '{"id":...}'
```

## 3. 发现

- 22 个 vendor FIX 后流程正常运行
- actor 字段正确路径：detail.activeTaskList[0].taskActorIdList
- approvalRecord 直接返回 list（无 list 包装）

## 4. 文档改进

- 已写入 `runner.py` 通用执行模板
- 后续测试流程可复用
