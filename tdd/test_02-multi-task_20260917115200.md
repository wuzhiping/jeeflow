# 02-multi-task 覆盖测试（按 AGENTS.md §5 全流程）

- **测试时间**：2026-09-17 11:52:00
- **测试类型**：按 docs/AGENTS.md §5 完整流程
- **关联流程**：`./flows/02-multi-task.json`

## 1. 设计概览

```
节点：start → apply(applicant) → task1(leader) → task2(manager) → task3(boss) → end
边：6 条串行
字段权限：task1/task2/task3 上 PERMISSION_f_project=1, PERMISSION_f_budget=2
```

## 2. 部署（§5.3）

| 步骤 | 结果 |
|---|---|
| reset | code:0 |
| save (name=multi-task) | code:0, data.id=9（UPSERT 复用 id） |
| deploy (id=9) | code:0, data.processDefineId=113 |

⚠️ reset 后 save+deploy 仍得到 processDefineId=113（累积自增，与 01 一致）

## 3. 启动（§5.4）

```bash
curl -X POST /wf/processInstance/startAndExecute -d '{
  "processDefineId": 113, "operator":"user1",
  "assignees":{"apply":"user1"},
  "variables":{"submitType":0,"f_applyUser":"user1","f_project":"P1","f_budget":100000,
               "u_userId":"user1","u_realName":"用户1"}
}'
```

→ instanceId=91764722167658

## 4. 执行（§5.5）

| 步骤 | task | operator | submitType | code |
|---|---|---|---|---|
| 1 | task1 | leader | 0 | 0 ✅ |
| 2 | task2 | manager | 0 | 0 ✅ |
| 3 | task3 | boss | 0 | 0 ✅ |

## 5. 校验（§5.6）

### 5.1 detail

```
state=20 (DONE)  active=0
  apply  state=20 actorIds=['user1']
  task1  state=20 actorIds=['leader']
  task2  state=20 actorIds=['manager']
  task3  state=20 actorIds=['boss']
```

### 5.2 approvalRecord

```
apply  operator=user1
task1  operator=leader
task2  operator=manager
task3  operator=boss
```

### 5.3 highLight

```
history=['apply', 'task1', 'task2', 'task3', 'end']  active=[]
```

## 6. AGENTS.md §10 自检

- [x] §5.3 save + deploy 返回 code:0
- [x] §5.4 startAndExecute 返回 processInstanceId
- [x] §5.5 走完所有 task，state==20
- [x] §5.6 approvalRecord 节点顺序与设计一致
- [x] §5.6 highLight history 节点序列符合设计
- [x] 所有 action 均在 §5.1 速查表
- [x] 测试日志落盘
- [x] 环境重置

## 7. 复盘

无新发现。流程行为完全符合设计：3 个 task 串行推进，每个 task 的 assignee 由 `properties.assignee` 字面量解析，无变量/handler 介入。**字段权限**（task1/task2/task3 的 PERMISSION_f_project/f_budget）未在本次覆盖（需前端校验）。

## 8. 结论

✅ **PASS** — 02-multi-task 全流程符合设计，state=20 DONE。
