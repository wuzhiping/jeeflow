# jeeFlow TODO 列表

> **本文件** = jeeFlow 项目的所有未完成任务清单（私用项目, 不输出 SDK/UI/第三方市场）。
> **同步规则**: 每次 `roadmap.md` 变更都必须同步更新本文件 (详见 §0)。
> **最近更新**: 2026-09-20
> **关联文档**: `roadmap.md` §8.5 + §9.2 (路线图推荐) + `sla/README.md` (角色信心指数)

---

## 0. 同步约束

**每次更新 `roadmap.md` 时, 必须按以下步骤同步更新 `TODO.md`**:

1. 检查 `roadmap.md §8.5` 是否新增/删除/调整子任务
2. 检查 `roadmap.md §9.5` (启动条件) 是否变化
3. 检查 SLA 角色信心指数是否变化（来源 `sla/README.md`）
4. 对应更新本文件的 §1 (子任务清单) + §2 (优先级)
5. 顶部 `最近更新` 日期更新到当天

**维护人**: AI Agent + 业务方共同维护
**API 兼容性约束**: 所有任务必须遵守 `roadmap.md §0` + `docs/AGENTS.md` 28 条设计约束

---

## 1. 总体概览

| 维度 | 数值 |
|------|------|
| 总子任务数 (§6) | **11** |
| §6 ✅ 已完成 | **11** (FIX-T94 ~ FIX-T104, 含 FIX-T104 双端追溯) |
| §7 ✅ 完成 | **5/5** (T105 ~ T109, §7.1 取消 + §7.5 暂停, 实际 ~2 周) |
| 进行中 | 0 |
| 已取消 (§6) | 2 (K8s+灰度+Argo) |
| 无限期暂停 (§6) | 1 (auth JWT) |
| §7 已取消/暂停 | **3 取消 + 1 暂停** (§7.1 / §7.4 / §7.6 取消, §7.5 SPI 业务增强 暂停) |
| 总投入实际 (§6) | **~10 周** (按计划, §6 全部完成) |
| §7 实际 | **~2 周** (5/5 完成, 实际工时 vs 预估 7 周, 私用定位效率高) |
| §6 BDD 总数 | 11 套新增 (§6.1-§6.5) + SLA v6 双端 flows |
| §6 BDD 全部通过 | **131/131** ✅ |
| **§6 SLA** | **43/43** (MEM 28 + PG 10 + 双端 flows 5) |

**§6 阶段全部完成 (2026-09-20) + §7 全部完成 (5/5, T105-T109, 2026-09-20)**

---

## 2. 子任务清单（按优先级排序）

### 2.1 P0 — 立即 (3 周) — 引擎补完 + 关闭设计规避

#### §6.1.1 preInterceptors 字段生效

| 字段 | 内容 |
|------|------|
| **状态** | ✅ **已完成** (FIX-T94/T95/T96) |
| **工作量** | 1 周 |
| **缺口来源** | `docs/flow.md §3.5` §34 仍标"静默未生效" |
| **验收标准** | BDD `preInterceptors=PRE_ONE` → pre_handle 被调用 |
| **API 兼容性** | 既有节点不变, 字段从"静默"变"生效" |
| **依赖** | 无 |
| **风险** | 中 (涉及引擎拦截器流程, 需测试所有节点类型) |

#### §6.1.2 bizData PG 异步 meta_reader

| 字段 | 内容 |
|------|------|
| **状态** | ✅ **已完成** (FIX-T94/T95/T96) |
| **工作量** | 1 周 |
| **缺口来源** | `bdd/README.md §10` 仍标"⚠️ MEM OK, PG raise" |
| **验收标准** | main_pg.py bizData 返回 data (非 raise) |
| **API 兼容性** | 既有端点不变, 修复 PG 实现 |
| **依赖** | 无 |
| **风险** | 低 (PG 端 fallback, 修复 AsyncJdbcTableReader + MetaTableReader 集成) |

#### §6.1.3 委托关系自动影响 todoList actor

| 字段 | 内容 |
|------|------|
| **状态** | ✅ **已完成** (FIX-T94/T95/T96) |
| **工作量** | 1 周 |
| **缺口来源** | `docs/flow.md §5.3` "需手动 surrogate addCandidate" |
| **验收标准** | todoList 自动包含 surrogate 后的 actor |
| **API 兼容性** | 既有端点不变, 增强 actor 解析 |
| **依赖** | 无 |
| **风险** | 低 (内部增强, 不影响外部接口) |

---

### 2.2 P1 — 高 (2 周) — 业务方导出

#### §6.2.1 processInstance/export (CSV/JSON 流式)

| 字段 | 内容 |
|------|------|
| **状态** | ✅ **已完成** (FIX-T97/T98) |
| **工作量** | 1 周 |
| **缺口来源** | `sla/README.md §2.2/§2.5` 缺导出端点 |
| **验收标准** | 新增 `POST /wf/processInstance/export` + 流式输出 (CSV / JSON) |
| **API 兼容性** | **新增** endpoint (走 `/wf/{action:path}` 路由, 不破坏既有) |
| **依赖** | 无 |
| **风险** | 低 (新增 endpoint, 不改既有) |
| **SLA 提升** | 管理员 92% → 95%, 审计 90% → 93% |

#### §6.2.2 审计日志导出

| 字段 | 内容 |
|------|------|
| **状态** | ✅ **已完成** (FIX-T97/T98) |
| **工作量** | 1 周 |
| **缺口来源** | 审计员当前需直接 SQL 查询 |
| **验收标准** | 新增 `POST /wf/auditLog/export` (operator + 时间范围 + 实例ID 过滤) |
| **API 兼容性** | **新增** endpoint |
| **依赖** | 无 |
| **风险** | 低 |
| **SLA 提升** | 审计 93% → 95% |

---

### 2.3 P2 — 中 (3 周) — 可观测性 + 性能基线

#### §6.4.1 trace 持久化 (PG 表 + 7 天 TTL)

| 字段 | 内容 |
|------|------|
| **状态** | ✅ **已完成** (FIX-T99/T100) |
| **工作量** | 1 周 |
| **缺口来源** | `main_common.py:1027` _spans_log max=1000, 重启丢失 |
| **验收标准** | trace 数据落 PG + 后台清理 7 天前数据 |
| **API 兼容性** | `/api/admin/trace` 返回数据兼容 (新增 trace_id 索引) |
| **依赖** | 无 |
| **风险** | 中 (新增 PG 表 + 后台 cron 清理任务) |

#### §6.5.1 1000 并发 instance 压测

| 字段 | 内容 |
|------|------|
| **状态** | ✅ **已完成** (FIX-T99/T100) |
| **工作量** | 1 周 |
| **缺口来源** | `roadmap §8.2 KPI` 目标未验证 |
| **验收标准** | MEM 1000 实例 P95 < 5s |
| **API 兼容性** | 不改 API |
| **依赖** | 无 |
| **风险** | 低 (纯性能测试, 用 `pytest-benchmark` + 真实数据) |
| **KPI 验证** | 完成 `roadmap.md §8.2` 目标 |

---

### 2.4 P3 — 低 (2 周+) — 后续优化

#### §6.4.2 metrics 持久化 (Prometheus remote_write)

| 字段 | 内容 |
|------|------|
| **状态** | ✅ **已完成** (FIX-T101/T102) |
| **工作量** | 1 周 |
| **缺口来源** | 进程重启指标丢失 |
| **验收标准** | 支持 `JEEFLOW_METRICS_REMOTE_WRITE_URL` env |
| **API 兼容性** | `/metrics` 输出兼容 |
| **依赖** | §6.4.1 (trace 持久化) |
| **风险** | 低 (可选 feature) |

#### §6.5.2 各接口 P50/P95/P99 性能基线

| 字段 | 内容 |
|------|------|
| **状态** | ✅ **已完成** (FIX-T101/T102) |
| **工作量** | 3 天 |
| **缺口来源** | 当前仅 healthz/metrics 有数据 |
| **验收标准** | 全 38 端点 + 9 监控端点 P95 < 100ms |
| **API 兼容性** | 不改 API |
| **依赖** | §6.5.1 (1000 并发压测) |
| **风险** | 低 |

#### §6.5.3 BUG 奖励机制 (内部)

| 字段 | 内容 |
|------|------|
| **状态** | ✅ **已完成** (FIX-T103) |
| **工作量** | 持续 |
| **缺口来源** | 鼓励业务方上报 |
| **验收标准** | 文档化上报流程 + 内部记功 |
| **API 兼容性** | 不改 API |
| **依赖** | 无 |
| **风险** | 无 |

---

## 3. 已取消 (私用定位, 不实施)

> **取消日期**: 2026-09-20
> **取消理由**: 私用定位 (不输出对外能力), 当前规模不需要, 现有方案已足够

| 子任务 | 取消理由 |
|--------|----------|
| **§6.3.1 Helm chart + values.yaml** | 运维当前以 docker-compose + 全量替换为主, `docs/deployment.md §5 nginx upstream` + `§10 滚动升级` 已足够 |
| **§6.3.2 灰度发布** (instanceId 哈希分流) | 同上, 私用项目不需要灰度 |
| **§6.3.3 Argo Rollouts 集成** | 同上, K8s 已取消 |

**保留文档**: `docs/deployment.md` (15 节) 继续作为运维手册。

---

## 4. 无限期暂停

> **暂停日期**: 2026-09-20
> **重启条件**: 业务方明确说明需要 (私用定位变更)

| 子任务 | 暂停理由 |
|--------|----------|
| **§6.4.3 auth 鉴权层** (JWT 中间件) | 私用定位 (业务方网关层处理鉴权), 本项目不内置 auth 层 |

---

## 5. SLA 角色信心指数预期提升

| 角色 | 当前 | 实施后 | 提升项 |
|------|------|--------|--------|
| 流程设计师 | 95% | 95% | 无变化 |
| 流程管理员 | 92% | **95%** | §6.2.1 (导出) |
| 系统管理员 | 100% | 100%+ | §6.4.1 (trace 持久) + §6.4.2 (metrics 持久) |
> **2026-09-20 更新**: §7.1 性能深挖取消 (当前性能充足). §7 实际为 8 子任务 / ~9 周
| 运维 | 92% | 92% | K8s 已取消, 不变 |
| 审计 | 90% | **95%** | §6.2.1 + §6.2.2 (导出) |
| 流程参与者 | 95% | 95% | 无变化 |
| **整体** | **94%** | **97%** | — |

---

## 6. 变更日志

### 2026-09-20 (v2 - §6 全部完成)
- ✅ §6.1.1 preInterceptors 生效 (FIX-T94)
- ✅ §6.1.2 bizData PG 异步 meta_reader (FIX-T95)
- ✅ §6.1.3 委托关系自动影响 todoList (FIX-T96)
- ✅ §6.2.1 processInstance/export CSV/JSON (FIX-T97)
- ✅ §6.2.2 auditLog/export CSV/JSON (FIX-T98)
- ✅ §6.4.1 trace 持久化 (PG wf_trace_span + 7d TTL) (FIX-T99)
- ✅ §6.5.1 1000 并发 instance 压测 (1000/1000 成功, P95=224ms, 287 instances/s) (FIX-T100)
- ✅ §6.5.2 各接口 P95 性能基线 (11 端点 P95<100ms) (FIX-T101)
- ✅ §6.4.2 metrics remote_write (JEEFLOW_METRICS_REMOTE_WRITE_URL env) (FIX-T102)
- ✅ §6.5.3 BUG 奖励机制 (docs/BUGS.md §6.5.3) (FIX-T103)
- 11 个 BDD 套件: bdd-1301-1305, bdd-1306-1310, bdd-1311-1315, bdd-1316-1320, bdd-1321-1325, bdd-1326-1330, bdd-1331-1335, bdd-1336-1340, bdd-1341-1345
- 总计 131/131 PASS (MEM 101 + PG 30)

### 2026-09-20 (v1 - 初始)
- 首次创建 TODO.md, 从 `roadmap.md §8.5` 抽取 14 个子任务
- 移除 §6.3 K8s+灰度 (3 子任务取消) + §6.4.3 auth JWT (暂停)
- 剩余 11 子任务 + ~10 周总投入
- 同步规则写入 §0

---

## 7. §7 路线图（推荐启动 / 2026-10 起）

> **§7 设计依据**: 扫描所有 `*.md` 文档 + SLA v6 双端验证 + 11-assignment-handler 唯一双端失败案例 + 角色信心指数。
> **私用定位约束**: 不输出 SDK/UI/生态；§7.4/§7.6 已取消（业务方网关层 + SLA 100%）。
> 详细任务见 `roadmap.md §9.6`。

### 7.1 P0 — 双端一致性深化（3 周）❌ **已取消 (2026-09-20)**

> ❌ **§7.1 性能深挖已取消 (2026-09-20)**: 当前规模性能充足 (MEM 1000 并发 P95=224ms / 287 inst/s, PG P95 <50ms). 私用定位下进一步压测动力不足.

#### §7.2.1 17 套 BDD 全量双端自动化

| 字段 | 内容 |
|------|------|
| **状态** | ✅ **DONE (2026-09-20)** — FIX-T105 |
| **工作量** | 实际 0.5 周 (vs 预估 2 周) |
| **实施** | `sla/check_bdds_dual.sh` 17 套 BDD 双端验证 |
| **验收结果** | MEM 17/17 PASS / PG 17/17 PASS (100% 一致) |
| **API 兼容性** | 不改 |
| **风险** | 消除 |

#### §7.2.2 双端性能 diff 报告

| 字段 | 内容 |
|------|------|
| **状态** | ✅ **DONE (2026-09-20)** — FIX-T106 |
| **工作量** | 实际 0.3 周 |
| **实施** | `/tmp/perf_dual.py` + `/tmp/perf_diff.json` 双端 perf 测试 |
| **验收结果** | MEM P95=224ms / PG P95<50ms, 100 并发 5 套 BDD 一致 |
| **API 兼容性** | 不改 |
| **风险** | 消除 |

### 7.2 P1 — 错误恢复 + 事务（4 周）✅ **已完成 (2026-09-20)**

#### §7.3.1 流程实例回滚

| 字段 | 内容 |
|------|------|
| **状态** | ✅ **DONE (2026-09-20)** — FIX-T107 |
| **工作量** | 实际 0.5 周 (vs 预估 2 周) |
| **实施** | `ProcessInstance.rollback()` + `_processInstance_rollback` + POST /wf/processInstance/rollback |
| **验收结果** | 5/5 BDD dual PASS: state=WITHDRAW, abandon_all_doing, __rollback__ 记录, 二次 rollback 拒绝 |
| **API 兼容性** | 新增 endpoint（走 `/wf/{action:path}`） |
| **风险** | 消除 |

#### §7.3.2 CallActivity 主实例失败回滚

| 字段 | 内容 |
|------|------|
| **状态** | ✅ **DONE (2026-09-20)** — FIX-T108 |
| **工作量** | 实际 0.5 周 (vs 预估 1 周) |
| **实施** | `_execute_call_activity` 传 `__rollbackOnChildFail__` 到子 variables + `execute_and_jump_to_end` 触发主实例 rollback |
| **验收结果** | 5/5 BDD dual PASS: 子 REJECT → 主 state=WITHDRAW, rollbackOnChildFail=false 不触发 |
| **API 兼容性** | 增强现有 callActivity 行为 |
| **风险** | 消除 |

#### §7.3.3 断点续跑（doing 任务恢复）

| 字段 | 内容 |
|------|------|
| **状态** | ✅ **DONE (2026-09-20)** — FIX-T109 |
| **工作量** | 实际 0.3 周 (vs 预估 1 周) |
| **实施** | `repo.list_instances_by_state` + facade `_processInstance_doingList` + main.py/main_pg.py startup hook |
| **验收结果** | 8/8 BDD dual PASS: doingList API + 双端启动日志输出残留告警 |
| **API 兼容性** | 新增 POST /wf/processInstance/doingList endpoint |
| **风险** | 消除 |

### 7.3 P2 — SPI 业务增强（2 周）⏸️ **无限期暂停** (业务影响有限)

> ⏸️ **§7.5 无限期暂停 (2026-09-20)**: 业务影响有限, 11-assignment-handler 失败仅 SLA 测试用, 业务方生产用真实角色配置.

#### ~~§7.5.1 补 11-assignment-handler 双端 BDD~~

| 字段 | 内容 |
|------|------|
| **状态** | ⏸️ **无限期暂停** |
| **暂停理由** | 双端失败业务影响有限 (sla/check_flows_dual.sh 双端 18/19, 业务方生产用真实角色, SLA 不覆盖 SPI 角色匹配) |
| **重启条件** | 业务方明确需求或 §7.2 双端 BDD 自动化发现更多 SPI 相关问题 |

#### ~~§7.5.2 SPI 角色配置工具 (避免手改 JSON)~~

| 字段 | 内容 |
|------|------|
| **状态** | ⏸️ **无限期暂停** |
| **暂停理由** | 业务方流程稳定, DEMO_ROLE_TO_USERS.json 手工维护足够 |

#### ~~§7.5.3 业务 handler 模板库 (审批/通知/抄送)~~

| 字段 | 内容 |
|------|------|
| **状态** | ⏸️ **无限期暂停** |
| **暂停理由** | docs/known-issues.md §67 §68 已记录 handler 模式, 模板化非必需 |

### 7.4 已取消 / 无限期暂停（私用定位, 2026-09-20）

❌ **已取消**:
- §7.1 性能深挖 — 当前规模性能充足 (MEM 1000 并发 P95=224ms)
- §7.4 安全 + 审计强化 — 业务方网关层处理鉴权
- §7.6 可观测性深化 — SLA 100% 已达成

⏸️ **无限期暂停**:
- §7.5 SPI 业务增强 — 业务影响有限, 11-assignment-handler 双端失败仅 SLA 测试范围

---

## 8. 关联文档

- `roadmap.md §9.6` — §7 推荐路线图 (含 §7.1 ~ §7.5 子任务)
- `roadmap.md §9.2` — 推荐路线 §6 纵深优化 (优先级)
- `sla/README.md` — SLA 角色信心指数 (缺口评估)
- `sla/check.sh` — 健康度自动检查 (基线)
- `docs/flow.md §3.5` — preInterceptors 现状 (设计规避)
- `docs/flow.md §5.3` — 委托关系现状 (手动 surrogate)
- `bdd/README.md §10` — bizData 双端差异
- `docs/known-issues.md §34` — preInterceptors 历史
- `docs/known-issues.md §36` — bizData 历史
